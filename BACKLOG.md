# Бэклог: «Панда будет?»

Мобильный тренажёр для игрока ЧГК. Бесплатный, некоммерческий, Flutter, офлайн.
Дистрибуция по образцу `~/speech-rehab` (APK вручную, в дальнейшем OTA).

**Источник правды — этот файл.**

Последняя ревизия — 29.08.2026. Документ переписан целиком: прежние три слоя (первичный дизайн → «Ревизия концепта» → разбор с проверкой по данным) склеены в одно. Каждая задача сформулирована в текущем виде; переигранные решения и причины — в разделе «История решений» внизу.

---

## Концепт

**Что это.** Тренажёр для одного человека. Не симулятор игры — команду и обсуждение он не заменяет. Он тренирует то, что человек приносит в команду сам.

### Навык раскладывается на три оси

Когда не берёшь вопрос, причина всегда одна из трёх:

| ось | пробел | чем тренируется |
|---|---|---|
| **факт** | не знал факта | наигранность, карточки эрудиции |
| **клише** | не узнал бинго | кампания по клише, сетка |
| **приём** | не увидел, как построен вопрос | приём недели |

Четвёртая причина — не хватило времени — это не пробел, а скорость.

Три разных пробела требуют трёх разных тренировок. Приложение — это три оси, а не набор режимов.

### Цикл вопроса — по турнирной конвенции

```
вопрос зачитывается            — читаешь текст, время не идёт
старт минуты                   — жмёшь сам, когда прочитал
50-я секунда                   — предупреждение «10 секунд»
минута кончилась
+10 секунд на запись           — сдаёшь версию
раскрытие                      — ответ, зачёт, комментарий, источник, автор
самооценка                     — взял / почти / не взял
```

Досрочное «готов отвечать» завершает обсуждение и ведёт в те же 10 секунд записи, а не сразу к раскрытию.

Запись версии до раскрытия — не приём против хиндсайт-байеса, а воспроизведение реальной сдачи ответа. То, что она заодно снимает «ну я почти это и подумал», — бесплатное следствие.

**Пустая версия — валидная запись.** Она отличает «не взял, и версии даже не родилось» (пробел в раскрутке) от «не взял, но шёл в верную сторону» (почти взятый вопрос).

### Диагностика-роутер

Один тап после раскрытия: **что помешало?** → `не знал факта` / `знал, но не связал` / `не увидел приём` / `не хватило времени`.

Это не статистика. Это то, что связывает приложение в целое: ответ решает, чем ты займёшься дальше. Показывается только при `почти` / `не взял` — при `взял` бессмысленна.

### Четыре занятия

**Классика** — поток случайных вопросов, набивается наигранность. Бесконечная.

**Бинго** — главное. Сетка 3×3, в клетках клише. Вопрос → своя версия → **и только потом** выбор клетки («к какой из девяти?» либо «ни к одной») → раскрытие. Угадал клише — клетка закрашена; взял вдобавок и сам вопрос — золотая. Линия по закрашенным любого цвета.

Конечная кампания: ~330 клише, однажды они кончатся. Так и задумано — у цели есть дно.

**Приём недели** — учебник. Карточка в понедельник, всю неделю выборочный вопрос «здесь он был?», в воскресенье разбор. Девятнадцать недель — девятнадцать приёмов.

**Вопрос дня** — один вопрос для всех в сутки, одна попытка. Ритуал, чтобы заходить.

### Два сквозных механизма

**Тап «это бинго?»** в обычной игре — узнавание без подсказки и без девяти вариантов, то есть как на живой игре. Он же единственный способ, которым база клише может расти.

**Справочник** — книга из трёх разделов (клише / приёмы / факты), которую заполняют три занятия. Открывается не из меню, а в момент промаха и по тапу на клетку сетки.

### Панда

Сокомандник, а не мотиватор. Мягкая круглая форма, приподнятая бровь. Подкалывает за всё — медленно читаешь, снова сидишь в два ночи, третий раз купился на один приём.

**Железное правило: за неверный ответ не язвит никогда.** Иначе выходит насмешка ровно в момент неудачи. Там — либо сухо по делу, либо в солидарность. Изредка, раз на ~30 реплик, говорит искреннее без сарказма: только редкость и продаёт, что команда его любит.

### Критерии успеха MVP

Названы заранее, иначе решение о следующем режиме принимается по настроению:

- возвращаюсь ≥5 дней из 7 без напоминания;
- растёт доля «взял» на собственных слабых местах;
- ловлю узнавание клише на живой игре.

---

## Рекомендуемая последовательность

Порядок обязателен там, где помечено «блокирует». Остальное — рекомендация.

### ✅ Фаза 0 — фундамент. Закрыта 30.08.2026

| № | задача | почему здесь |
|---|---|---|
| 1 | ✅ **T10** журнал событий | T2a — единственный писатель в журнал. Заложить сразу бесплатно, ретрофитить — переписывать все режимы |
| 2 | ✅ **T11** санитайзер дампов | Нельзя показывать вопросы, среди которых каждый восьмой непригоден |

Итог: `app/lib/journal/` пишет типизированные события, `data/gq_clean.json` (8866 играбельных) и `data/bingo_clean.json` (599 / 132 темы) готовы к раздаче в приложение.

### ✅ Фаза 1 — первый сквозной срез. Код закрыт 30.08.2026, идёт контрольная точка

| № | задача | примечание |
|---|---|---|
| 3 | **T2c** шаг 1 — APK в CI | **до старта остального.** Обкатываем на телефоне с первого дня, а не в конце: канал «собралось → поставилось → журнал пишется» проверяется на скелете, пока цена ошибки нулевая |
| 4 | **T2a** цикл вопроса | движок; его же переиспользуют T3 и T4b |
| 5 | **T2b** «Классика» end-to-end | ассеты, отбор, раунд из пяти. Первый режим, на котором всё проверяется |
| 6 | **T4a** тап «приём недели» + первая карточка | едет вместе с Классикой, не после |
| 7 | тап «это бинго?» | часть T2a, но проверяется здесь |
| 8 | **T2c** шаг 2 — отладочный экран | после T2b: раньше в журнале нечему накапливаться |
| 9 | ⏳ **T17** название на главном экране | код готов 30.08.2026, ждёт пуша и взгляда на телефон |
| 10 | **T18** блокнот версий во время минуты | оттуда же; внутри развилка — решить до кода |

Все шесть строк закрыты 30.08.2026, ручные проверки на телефоне пройдены: раунд из пяти end-to-end, `journal.jsonl` переживает перезапуск, экран за минуту не гаснет, `loadAll()` 871 мс, карточка урока читается за полминуты.

**→ Контрольная точка: две недели пользоваться самому. Идёт с 30.08.2026.** Сверить с критериями успеха — числа берутся с отладочного экрана (долгий тап по заголовку). Всё, что дальше, решается по результатам, а не заранее.

### Параллельно, никого не блокирует

| задача | примечание |
|---|---|
| ✅ **T16** импорт индекса бинго + картинки | закрыта 30.08.2026 — корпус бинго вырос втрое, раздатка вернулась |
| **T8** арт панды | долгая работа, начинать можно когда угодно |
| ✅ **T19** самообновление APK | закрыта 30.08.2026 — сборка доезжает до телефона кнопкой, без перекладывания файла |

### Фаза 2 — вторая ось

| № | задача | зависит от |
|---|---|---|
| 7 | **T14** справочник | ✅ T16 (контент и картинки готовы), T10 (состояния) |
| 8 | **T3** сетка «Бинго» | ✅ T16 (корпус вырос до 1764 вопросов и 328 тем), T2a |
| 9 | **T4b** полный цикл недели + гаунтлет | T4a, накопленная разметка |

### Фаза 3 — ритуал и профиль

| № | задача | примечание |
|---|---|---|
| 10 | **T12** вопрос дня | день работы, можно и раньше |
| 11 | **T9** профиль и метрики | нужен накопленный журнал, раньше нечего показывать |

### Потом

**T15** ось фактов (карточки из экселя) · **T6** «Своя игра» · **T13** — закрыт слиянием в T16.

**Правило на время фазы 1:** до того как «Классика» отработает end-to-end на реальных данных, в этот файл пишутся только исправления фактических ошибок — не новые дизайны. Сейчас в репозитории 138 строк Dart и ~11 тысяч вопросов, которые никто не читает; концепт уточняется быстрее, чем строится.

---

## Задачи

### Закрытые

| ID | задача | результат |
|---|---|---|
| ✅ T1 | Структурировать дамп bingo-вики | `data/bingo_questions.json`, 635 вопросов / 135 тем, скрипт `scripts/structure_bingo_dump.py`. ⚠️ проза статей выброшена — забирается обратно в T14; мойибейк не починен — в T11 |
| ✅ T2 | Flutter-скелет | меню-карточки, заглушки режимов, CI на GitHub Actions (web + тесты), превью на github.io |
| ✅ T5 | Пайплайн «Классики» | `data/gotquestions_dump.json`, 150 пакетов / 9280 вопросов |
| ✅ T7 | Название | «Панда будет?» |
| ✅ T11 | Санитайзер дампов | `scripts/sanitize_dumps.py` → `data/*_clean.json`. Мойибейк оказался багом нашего скрапера — починен, вики-дамп пересобран. Подробности в разделе ниже |
| ✅ T10 | Журнал событий | `app/lib/journal/` — типизированные события в JSONL, свёртки вместо хранилищ прогресса. Схема и развилки — в разделе ниже |
| ✅ T16 | Импорт индекса бинго (поглотил T13) | 199 статей четырёх хостов → корпус бинго 599 → **1764** вопроса, тем 132 → **328**, тем с 3+ вопросами 103 → **286**. Картинок 771, из них 105 раздаток — раздаточный материал вернулся. Скрипты `crawl_bingo_index.py`, `structure_bingo_index.py`, `fetch_bingo_images.py`, `bingo_index_report.py`. Подробности в разделе ниже |
| ✅ T19 | Самообновление APK | кнопка в шапке главного экрана: CI кладёт подписанный APK и манифест в релиз с тегом `auto`, приложение сравнивает build-номер и ставит свежую сборку. Сквозной прогон на телефоне пройден 30.08.2026 |

---

### Приложение

#### ✅ T10 — журнал событий

**Закрыта 29.08.2026.** Реализовано в `app/lib/journal/` — `event.dart`, `event_log.dart`, `event_log_io.dart`, `event_log_factory*.dart`, `projections.dart`, `journal_scope.dart`; 28 тестов зелёные, `flutter analyze` чист, `flutter build web` собирается. Схема события ниже — действующий контракт, а не план: T2a пишет ровно эти поля.

**Проблема:** три независимых хранилища прогресса (темы, XP, Leitner) — гарантированный рассинхрон, каждая новая метрика требует нового поля.

**Решение:** один append-only журнал типизированных событий. Все метрики — свёртка журнала, ничего не хранится параллельно: наигранность, X/330 клише, X/19 приёмов, карта слабых мест, отбор проваленных на повтор. Новую метрику можно ввести задним числом, пересчитав историю.

##### Как сейчас

`app/lib` — 126 строк скелета T2: `main.dart`, `app_theme.dart`, `screens/home_screen.dart`, `widgets/coming_soon_screen.dart`. Ни состояния, ни персистентности, ни единой зависимости кроме `flutter`/`flutter_test`. `assets:` в `pubspec.yaml` не объявлены — дампы в сборку пока не едут. CI (`.github/workflows/build.yml`) гоняет `flutter test` и собирает **только web** (превью на github.io); платформенные папки генерируются на бегу через `flutter create`.

Проверено по дампам, важное для схемы:

- ID вопросов есть в обоих корпусах и **не сталкиваются**: `gq-369291` (9280 шт., уникальны) и `15-minut-slavy-1` (635 шт., уникальны), пересечение ID-пространств — 0. `questionId` можно хранить как одну строку без префикса-неймспейса;
- `theme` в `gotquestions_dump.json` — **null у всех 9280**. Поле `theme` события осмысленно только для бинго-корпуса;
- 10 вопросов физически совпадают текстом под разными ID (`aporii-zenona-9` ↔ `gq-345259` и др.). Для журнала это два разных вопроса; схлопывание — работа T11, не T10.

##### Схема события

Одна строка JSON на событие (JSONL). Общие поля у всех типов:

```
v          int      версия схемы, начинается с 1
type       string   answer | note | sessionStart   (tagSuggestion — зарезервировано)
ts         int      epoch millis UTC
day        string   YYYY-MM-DD локальной даты В МОМЕНТ ЗАПИСИ
```

`day` пишется отдельным полем, а не вычисляется из `ts` при чтении: локальную дату задним числом из UTC не восстановить, если таймзона сменилась. На ней держатся streak (T9) и «одна попытка в сутки» (T12) — то есть ровно календарная арифметика, которая тихо ломается на границе.

Тело `type: "answer"`:

```
questionId       string   gq-369291 | 15-minut-slavy-1
corpus           string   gq | bingo
mode             string   classic | bingo | tehnika | daily | facts
verdict          string   taken | almost | missed
secondsUsed      int      0..60, сколько минуты израсходовано до «готов»/истечения
answerWindowSec  int      длина окна записи, действовавшая на этом событии (по умолчанию 10)
userAnswer       string   текст версии; "" — валидное значение, не ошибка
reason           string?  fact | link | tehnika | time | null
hintUsed         bool
roundId          string?  epoch millis старта раунда, строкой; null вне раунда
theme            string?  настоящее клише (у gq всегда null)
themeGuess       string?  null = клетку не спрашивали; "" = «ни к одной»; иначе имя темы
tehnikaGuess     bool?    null = не спрашивали
tags             string[] предложения разметки из тапов, по умолчанию []
```

`corpus` дублирует то, что выводится из формы ID, — и стоит того: T3 мешает два корпуса в одном раунде, а свёртка «сколько дефицитного бинго-корпуса израсходовано» иначе обязана ходить в дампы.

`answerWindowSec` пишется по той же причине, по которой пишется `day`: T2a прямо объявляет длину окна записи настраиваемым параметром, а журнал, по которому нельзя понять, какие правила действовали в момент события, отменяет главную посылку T10 — «новую метрику можно ввести задним числом». Подкрутили окно с 10 до 15 секунд — доля `reason: time` до и после становится несопоставимой, и восстановить, где проходит граница, будет неоткуда.

Тело `type: "note"` (T14): `theme`, `text`, `questionId?`.
Тело `type: "sessionStart"` (T9, streak): пусто — пишется один раз на холодный старт, дедуп по `day` делает свёртка.

##### Шаги

Порядок — по цене переделки: сначала то, что задаёт форму данных.

1. **`lib/journal/event.dart`** — `JournalEvent` (sealed-иерархия или один класс с `type` и nullable-телом) + `toJson`/`fromJson`.
   → проверка: round-trip `fromJson(toJson(e)) == e` для всех трёх типов, отдельными кейсами `userAnswer: ""`, `themeGuess: ""`, `reason: null`, `tags: []`.

2. **`lib/journal/event_log.dart`** — интерфейс и результат чтения:
   ```dart
   abstract class EventLog {
     Future<void> append(JournalEvent e);
     Future<JournalRead> readAll();
   }
   class JournalRead { final List<JournalEvent> events; final int skippedLines; }
   ```
   `skippedLines` обязателен: битая строка и пустой журнал не имеют права схлопнуться в одно значение (`pitfalls_silent_failures` п.7). Плюс `MemoryEventLog` — для тестов и для web.
   → проверка: append → readAll возвращает то же и в том же порядке.

3. **`lib/journal/event_log_io.dart`** — `FileEventLog` на `dart:io`, **путь инжектируется в конструктор** (это seam для тестов), фабрика по умолчанию — `getApplicationDocumentsDirectory()/journal.jsonl`. Дозапись строкой в конец, без перечитывания файла. Три вещи, которые легко не сделать и которые потом стоят потерянных событий:

   - **записи сериализуются очередью** (последний `Future` в поле класса). Два незавершённых `append` подряд в одном изоляте — это два конкурирующих `writeAsString(mode: append)` и шанс получить перемешанные куски двух строк;
   - **оборванный хвост чинится при открытии:** если файл не кончается на `\n` (краш посреди записи), дописать `\n` перед первым новым событием. Иначе следующее событие приклеится к обрывку и битой станет не одна строка, а две — то есть потеряется в том числе валидное новое событие;
   - **несуществующий файл — это пустой журнал, а не ошибка:** первый запуск возвращает `JournalRead([], 0)`. Но глотать так можно **только** отсутствие файла: любая другая `FileSystemException` (нет прав, каталог вместо файла) пробрасывается наружу — иначе сбой чтения неотличим от «игрок ещё не играл», и весь накопленный прогресс молча показывается как нулевой.

   Чтение: построчно, каждая строка в `try` — исключение на строке инкрементит `skippedLines` и не роняет чтение; строка с `v` больше известной — туда же.
   → проверка: (а) три append, затем **новый экземпляр** над тем же файлом видит три события — переживает перезапуск; (б) файл с битой строкой посередине → 2 события и `skippedLines == 1`, исключения нет; (в) файл, оборванный на середине строки, + новый append → 1 событие и `skippedLines == 1`, новое событие читается целым; (г) `readAll` по несуществующему пути → пусто без исключения.

4. **`lib/journal/event_log_factory.dart`** — conditional export:
   ```dart
   export 'event_log_factory_io.dart'
       if (dart.library.js_interop) 'event_log_factory_web.dart';
   ```
   Web получает `MemoryEventLog`. Условие — `dart.library.js_interop`, не `dart.library.html`: последний ломается при компиляции в wasm. Новая зависимость ровно одна — `path_provider`.
   → проверка: `flutter build web` в CI зелёный; `flutter test` проходит на обеих ветках.

5. **`lib/journal/projections.dart`** — чистые функции `List<JournalEvent> → результат`, без диска: `dueQuestions`, `streakDays`, `takenRate`, `knownThemes`. Правило возврата проваленного — одной таблицей констант в начале файла: `missed` → +2 дня; `almost` или `taken + hintUsed` → +7; повторный `missed` того же вопроса → +1. `taken` без подсказки в очередь возврата не попадает вовсе.
   → проверка: событие `missed` вчерашнее — не в выдаче `dueQuestions(now)`, оно же трёхдневной давности — в выдаче. Границы проверять включительно.

6. **`JournalScope extends InheritedWidget`** + запись `sessionStart` в `main.dart`.
   → проверка: виджет-тест поверх `MemoryEventLog` — после старта приложения в журнале ровно одно событие `sessionStart`.

##### Развилки — закрыты 29.08.2026

| развилка | выбор | почему |
|---|---|---|
| плоское «событие ответа» vs типизированные | **типизированные, `type` с первого дня** | T14 пишет заметку в журнал через минуты после того, как событие ответа уже легло в append-only лог; T9 считает streak по «дням, когда заходил», а не «отвечал». Ретрофит дискриминатора = миграция всей истории |
| хранилище | **JSONL-файл + in-memory заглушка на web** | append-only файл — ровно та форма, что описана в задаче. Web-превью из T2 остаётся живым (прогресс там не переживает перезагрузку — это демо). БД отвергнута: журнал — одна таблица с одним индексом, а объём ~50 событий/день ≈ 18k/год ≈ 5 МБ; `shared_preferences` отвергнут — перезапись всего лога на каждый ответ |
| `reason` (диагностика промаха) | **только при `almost`/`missed`, пропуск разрешён (null)** | «почему взял» смысла не имеет; обязательность даёт до пяти лишних тапов на раунд в самый неприятный момент и провоцирует жать первое попавшееся — такие данные хуже отсутствующих |
| `tagSuggestion` как отдельный тип | **в MVP не пишется** | тапы разметки внутри цикла вопроса едут полем `tags` события `answer`, как и записано в исходном T10. Имя в перечислении зарезервировано под тап из справочника (T14) |
| `themeGuess` = «ни к одной» | **пустая строка**, `null` = не спрашивали | имя темы пустым быть не может; отдельное булево поле-спутник даёт два источника правды об одном ответе |
| проброс журнала в дерево виджетов | **`InheritedWidget`, ~20 строк, без пакета** | достаточно для единственного потребителя в T10. **T2a вправе пересмотреть** под свои нужды состояния — это не приговор архитектуре приложения |
| писать ли настройки, действовавшие в момент события | **да, `answerWindowSec`** | это единственный параметр цикла, который T2a объявляет настраиваемым, и он меняет смысл `reason: time`. Один int против невосстановимой границы в истории |
| кеш свёрток | **нет, полная свёртка при старте** | 18k событий в год читаются и сворачиваются мгновенно. Снапшот вводить, когда журнал перевалит ~50k событий, — не раньше и не «на будущее» |

##### Зависимости

Ничего не требует. **Блокирует T2a и всё, что за ним.** T11 идёт параллельно и не пересекается. Новых пакетов ровно один — `path_provider`.

##### Критерий успеха

- `flutter test` зелёный локально и в CI, `flutter build web` собирается;
- **красный→зелёный на наивной реализации:** тест «битая строка посреди файла» падает, если декодировать строки в лоб без `try` (исключение рушит чтение всего журнала), и проходит на целевой; тест «новый экземпляр над тем же файлом видит записанное» падает на чисто in-memory реализации;
- ручной проверки не требуется: устройство не нужно, всё покрывается автотестами.

#### ✅ T2a — цикл вопроса (общий движок всех режимов)

**✅ Закрыта 30.08.2026.** `lib/model/question.dart`, `lib/cycle/{answer_match,cycle_controller,question_cycle,screen_wakelock}.dart`, тесты зелёные, `flutter analyze` чист. **Шаг 6 проверен на телефоне 30.08.2026: за раунд из пяти вопросов экран не гас ни разу.**
**Постановка проработана 29.08.2026.** Развилки закрыты, замеры — по `data/gq_clean.json` в состоянии после `653ccb4`.

Компонент параметризован вопросом и колбэком; колбэк отдаёт **полное событие в журнал**, не `correct: bool`. Цикл — по турнирной конвенции, см. «Концепт». Режимо-специфичного в нём нет ничего: T3 и T4b получают тот же виджет с другой конфигурацией.

##### Как сейчас

`app/lib/journal/` — закрытый T10: `EventLog.append/readAll`, `MemoryEventLog`, `FileEventLog`, `JournalScope.of(context)`, свёртки `dueQuestions/currentStreak/takenRate/recognizedThemes`. `AnswerEvent` уже несёт все поля цикла — `secondsUsed`, `answerWindowSec`, `userAnswer`, `reason`, `hintUsed`, `roundId`, `themeGuess`, `tehnikaGuess`, `tags`, — то есть схему трогать не нужно ни в одном шаге ниже. Единственный писатель сейчас — `main.dart` (`sessionStart`). Экраны: `home_screen.dart` ведёт три пункта меню в `ComingSoonScreen`, состояния нет, пакет один — `path_provider`.

##### Форма

`CycleController extends ChangeNotifier` + виджет `QuestionCycle`. Часы **инжектируются** (`DateTime Function() now`) — это seam для тестов, без него ни один тест таймера не пишется. Фазы:

```
reading   — читаешь текст, время не идёт, кнопка «начал»
thinking  — минута; блокнот версий; кнопка «готов отвечать»
writing   — окно записи answerWindowSec (по умолчанию 10), поле блокируется по истечении
bingoTap  — «узнал клише? назови» (config.askBingoTap) — ДО раскрытия
reveal    — ответ, зачёт, комментарий, источник, автор; рядом — блокнот версий
verdict   — самооценка взял/почти/не взял, предзаполненная матчером
reason    — диагностика-роутер, только при почти/не взял; пропуск разрешён
tehnika   — «здесь был приём недели?» (config.tehnika != null), см. T4a
```

Порядок `bingoTap` до раскрытия — не косметика: после раскрытия догадка о клише перестаёт быть догадкой. Тот же порядок, что в T3.

Цикл заполняет: `questionId`, `verdict`, `secondsUsed`, `answerWindowSec`, `userAnswer`, `reason`, `themeGuess`, `tehnikaGuess`, `hintUsed` (в фазе 1 всегда `false`). Режим заполняет: `mode`, `corpus`, `roundId`, `theme`. Собранное событие уезжает в `onFinished(AnswerEvent)` — **пишет в журнал режим, не цикл**: у T3 и T12 разные правила записи (одна попытка в сутки, раунд смешанного корпуса), а цикл обязан остаться неведающим.

##### Шаги

Порядок — по цене переделки: сперва форма данных и чистая логика, виджет последним.

1. **`lib/model/question.dart`** — `Question` (`id`, `corpus`, `question`, `answer`, `acceptance?`, `acceptVariants`, `comment?`, `sources`, `author?`) + `fromJson`. Все опциональные поля читать через `.get`-семантику (`j['comment'] is String ? ... : null`), как это уже сделано в `journal/event.dart`: в корпусе **2719 вопросов без `comment`, 1686 без `sources`, 460 без `author`** — прямое обращение по ключу упадёт на каждом третьем.
   → проверка: round-trip на записи из реального ассета и отдельным кейсом — на записи без `comment`/`sources`/`author`.

   **Сделано 30.08.2026, два отклонения от списка полей выше.** Первое: в `Question` добавлено поле `theme`. Без него постановка противоречит сама себе — «режим заполняет `theme`» невыполнимо, если цикл не откуда его взять; ровно так же режим берёт из `Question` и `corpus`. Второе: замеры в этом абзаце устарели после пересборки дампов в T11 — сейчас `comment` пуст у 2769, `sources` пуст у 1710, `author` — у 470. Вывод не меняется, цифры сдвинулись. Тесты читают `../data/*_clean.json` напрямую: ассет объявляет T2b, а проверять парсер на выдуманных записях смысла нет.

2. **`lib/cycle/answer_match.dart`** — чистая функция `matchAnswer(String userAnswer, List<String> acceptVariants) → MatchHint {taken, almost, none}`. Нормализация: нижний регистр, `ё→е`, снять пунктуацию и внешние кавычки, схлопнуть пробелы. Правило:
   - точное совпадение с любым вариантом, либо расстояние Левенштейна ≤ `max(1, длина ÷ 6)` (опечатка) → `taken`;
   - ответ игрока содержит вариант или вариант содержит ответ **при длине ответа ≥ 4 символов** → `almost`;
   - иначе `none`, и на экране не предвыбрано ничего.

   Порог длины обязателен: без него «а» — подстрока почти любого ответа.
   → проверка, **красный→зелёный:** пустой ответ не матчится ни с чем и никогда (на наивной реализации `''.contains` даёт `almost` на каждом вопросе, то есть пустая версия молча превращается в «почти»); «Уорхол.» → `taken` по варианту «уорхол»; «Уорхал» → `taken` по опечатке; «гамма» ↔ «гамма (музыка)» → не `none`.

3. **`lib/cycle/cycle_controller.dart`** — фазы, часы, сборка события. `answerWindowSec` — константа `kDefaultAnswerWindowSec = 10` в одном месте и параметр конструктора; экрана настроек в фазе 1 нет, но подкрутка длины окна не должна требовать правок в нескольких файлах — ради этого длина и пишется в каждое событие. Секунды считаются **как разница `now()`**, `Timer.periodic` только перерисовывает: таймер, у которого источник правды — счётчик тиков, при заморозке в фоне тихо врёт в пользу игрока, и заметить это по журналу невозможно.
   → проверка на подменённых часах: «готов» на 12-й секунде → `secondsUsed == 12`; окно записи ровно `answerWindowSec` и по истечении поле заблокировано, а набранное сохранено; минута истекла без «готов» → `secondsUsed == 60`. **Красный→зелёный:** тест «часы перескочили на 70 секунд вперёд, тиков не приходило» проходит на разнице `now()` и падает на счётчике тиков.

4. **`lib/cycle/question_cycle.dart`** — виджет по фазам, блокнот версий (`List<String>` в контроллере, в журнал не едет), кнопка «готов отвечать», поле записи, экран раскрытия, кнопки самооценки с предвыбором из шага 2, роутер причины.
   → проверка виджет-тестами: полный проход reading→done отдаёт в `onFinished` событие с непустым `questionId` и заполненным `answerWindowSec`; пустая версия доходит до события как `userAnswer: ''`, а не блокирует переход.

5. **Тап «это бинго?»** (`config.askBingoTap`) — одна строка «узнал клише? назови» + поле свободного ввода перед раскрытием. Непустой текст → `themeGuess: <текст>`; поле оставлено пустым → `themeGuess: null`. Пустая строка `''` в это поле **не пишется**: по контракту T10 она значит явное «ни к одной», а это осмысленный выбор только там, где показаны девять клеток (T3).
   → проверка: ввёл «Ковентри» → в событии `themeGuess == 'Ковентри'`; ничего не ввёл → `themeGuess == null`.

6. **`wakelock_plus`** — экран не гаснет с начала `thinking` до `done`, отпускается в `dispose` и при уходе в фон.
   → проверка ручная, автотестом не покрывается: на телефоне минута проходит без блокировки экрана. Записать в бэклог факт проверки.

##### Развилки — закрыты 29.08.2026

| развилка | выбор | почему |
|---|---|---|
| подсказка с 30-й секунды | **в MVP нет вовсе** | «подсвечивается фраза-триггер либо называется приём» требует разметки приёмов, а её ноль и авторазметка не работает (см. «Авторазметка приёмов не работает»). Подсказка появится в T4b, когда разметка накопится из игры. Следствие, которое надо помнить: `hintUsed` всю фазу `false`, а ветка `taken + hintUsed → +7 дней` в `projections.dart` недостижима — это не баг, чинить нечего |
| тап «это бинго?» | **свободный ввод названия клише → `themeGuess`** | по концепту тап — единственный способ, которым база клише может расти; флаг без имени не растит ничего. Разнобой формулировок сворачивается по нормализованному тексту в T16 |
| блокнот версий | **эфемерный, в журнал не едет** | схема T10 не трогается, а потребителя у черновиков нет ни одного: ни T9, ни T14 их не читают. Сбор данных без заявленного потребителя — ровно то, от чего предостерегает анти-бэклог. Поле `drafts[]` добавляется совместимо вниз в любой момент, когда потребитель появится |
| таймер и экран | **`wakelock_plus`, время по разнице `now()`** | минуту думаешь не касаясь экрана, а автоблокировка на типичном телефоне срабатывает через 30 секунд — минуту: без wakelock режим ломается на ровном месте. Пауза при уходе в фон отвергнута — в тренажёре для себя это легальный способ остановить минуту и подумать |
| автопроверка | **предзаполняет только положительный матч** | `acceptance` заполнен у 28%, промах матчера — норма, а не сбой. Предвыбранное «не взял» систематически воровало бы корзину «почти» — ту самую, которая диагностически ценнее всех |
| кто пишет в журнал | **режим, цикл только собирает событие** | у T12 «одна попытка в сутки», у T3 смешанный корпус в раунде — правила записи разные, а цикл обязан о режимах не знать |
| проброс состояния | **`ChangeNotifier` + `JournalScope`, без пакета** | единственный экран с состоянием; `provider`/`riverpod` вводить под один контроллер — абстракция под однократное использование |

##### Зависимости

T10 закрыт — журнал готов, схему менять не нужно. **Блокирует T2b, T4a, T3, T4b.** T11 не пересекается (цикл получает уже готовый `Question`). Новый пакет ровно один — `wakelock_plus`.

##### Критерий успеха

- `flutter test` и `flutter analyze` зелёные, `flutter build web` собирается;
- **красный→зелёный** на трёх тестах: пустой ответ не даёт `almost`; часы, перескочившие вперёд без тиков, дают истёкшую минуту; событие несёт `answerWindowSec` и `roundId`;
- ручная проверка ровно одна — экран не гаснет за минуту на телефоне; всё остальное покрывается автотестами.

#### ✅ T2b — «Классика» end-to-end

**Закрыта 30.08.2026,** все шесть шагов. **✅ Проверено на телефоне 30.08.2026: раунд из пяти проходится end-to-end, `journal.jsonl` пережил перезапуск, `loadAll()` — 871 мс.**
**Постановка проработана 29.08.2026.**

Первый режим, на котором проверяется всё остальное: ассеты, отбор, раунд, запись в журнал. Поток случайных вопросов, набивается наигранность, конца у режима нет — конец есть у раунда.

##### Как сейчас

Вопросов в приложении нет вообще: `assets:` в `pubspec.yaml` не объявлены, `app/assets/` не существует. Годный корпус лежит в `data/gq_clean.json` — **8866 вопросов** с `excluded == null`, у каждого непустой `acceptVariants`.

⚠️ **Ассет обязан лежать внутри `app/`, и это проверено, а не предположено.** Пустой пакет с `assets: - ../outside/data.json` собирается **зелёным**, ключ попадает в `AssetManifest.bin`, а файл в `build/web/assets/` не копируется — то есть `rootBundle.loadString` упадёт в рантайме, а сборка об этом не скажет. Обратный случай ведёт себя правильно: ассет объявлен внутри пакета, но файла нет → `flutter test` падает на `No file or variants found for asset` ещё до тестов. Отсюда порядок шагов: генерация ассета — до `flutter test` в CI.

##### Шаги

1. **`scripts/build_app_assets.py`** — `data/gq_clean.json` → `app/assets/questions.json`. Только `excluded == null`; поля `id, question, answer, acceptance, acceptVariants, comment, sources, author` — **8.55 МБ на 8866 вопросов** (замерено). `tournament` и `complexity` не едут: экран раскрытия их не показывает, а `complexity` вдобавок нельзя трактовать как процент взятия (у 2082 одно число, у 861 два — см. «Состояние контента»). `acceptance`-болванки «точный ответ» / «по смыслу» вырезаются — среди годных таких **397 из 2448 непустых**, показывать «зачёт: точный ответ» бессмысленно. Файл — объект `{"v": 1, "count": N, "questions": [...]}`, а не голый массив: `count` ловит обрезанный файл, который иначе распарсится как валидный короткий список.
   → проверка: тест в `scripts/tests/` — болванок в выводе нет, `count` равен длине списка и равен числу `kept` из `data/sanitize_report.json`.

   **Сделано 30.08.2026.** `scripts/build_app_assets.py` + `scripts/tests/test_build_app_assets.py` (11 проверок). **Отклонение от списка полей: добавлен `corpus`.** Без него `Question.fromJson` не собирает вопрос — а именно из `Question` режим и берёт `corpus` для события. Цена — 8.66 МБ вместо замеренных 8.55. Болванок вырезано ровно 397 из 2448, как и было замерено.

2. **`app/pubspec.yaml`** — `assets: - assets/questions.json`; в `.gitignore` — `app/assets/*.json`; в `.github/workflows/build.yml` — шаг `python3 scripts/build_app_assets.py` сразу после checkout (до `flutter pub get` и `flutter test`) и `paths:` расширяется на `data/**` и `scripts/**`, иначе перегенерация корпуса не будет пересобирать превью.
   → проверка: CI зелёный; локально удалённый `app/assets/questions.json` роняет `flutter test` с внятным сообщением.

3. **`lib/data/question_repository.dart`** — `abstract class QuestionRepository { Future<List<Question>> loadAll(); }` + `AssetQuestionRepository` (`rootBundle.loadString` → `compute(jsonDecode)`). Расхождение `count` с длиной списка и отсутствие ассета — исключение с текстом «сгенерируй ассет: `python3 scripts/build_app_assets.py`», которое экран показывает как ошибку. Пустой список молча не возвращать никогда: «вопросы кончились» и «ассет не собран» обязаны выглядеть по-разному.
   → проверка: тест грузит настоящий ассет через `rootBundle` и проверяет, что вопросов > 8000 и у каждого непустой `acceptVariants`. Этот же тест — тот самый гард против забытой генерации.

   **Сделано 30.08.2026, гард проверен мутацией:** убранный `app/assets/questions.json` роняет `flutter test` на «No file or variants found for asset» — до запуска тестов, как и предполагалось. ⚠️ Грабли на будущее: тест ассета обязан быть обычным `test()`, а не `testWidgets` — последний крутит фейковые часы, и Future от `compute()` в них не завершается никогда; тест не падает, а висит десять минут до таймаута.

4. **`lib/screens/classic_screen.dart`** — раунд из 5 вопросов. `roundId` = epoch millis старта раунда строкой. Отбор: при входе в режим `readAll()` → множество уже виденных `questionId` и `dueQuestions(events, now)`; **первый слот раунда — созревший вопрос, если он есть, остальные четыре — случайные из невиденных**; невиденные кончились (через ~1770 раундов) — берутся самые давние. Событие пишется **после каждого вопроса**, а не в конце раунда: краш на четвёртом не имеет права стоить трёх предыдущих ответов.
   `JournalRead.skippedLines > 0` — не молчать: экран режима показывает строку «часть журнала не прочитана (N строк)». Отбор опирается на «уже виденные», и частично прочитанный журнал тихо вернёт в поток то, что игрок уже проходил, — ровно тот случай, ради которого `skippedLines` и заведён в T10.
   → проверка: виджет-тест поверх `MemoryEventLog` и репозитория-заглушки — раунд из 5 кладёт 5 событий с одним `roundId`; журнал с битой строкой показывает предупреждение, а не молчит; вопрос, отвеченный в предыдущем раунде, во втором не выпадает; событие `missed` трёхдневной давности попадает первым слотом.

5. **Итоговый экран раунда** — пять строк с вердиктами, доля взятых, «ещё раунд» и «в меню». Место для реплики панды — пока просто текст, арт едет в T8.
   → проверка: после пятого вопроса показан итог, «ещё раунд» стартует раунд с новым `roundId`.

6. **`home_screen.dart`** — «Классика» ведёт в режим вместо `ComingSoonScreen`. Два оставшихся пункта пока остаются заглушками.
   → проверка: существующий `home_screen_test.dart` обновлён, не удалён.

##### Развилки — закрыты 29.08.2026

| развилка | выбор | почему |
|---|---|---|
| форма ассета | **один `questions.json`, грузится целиком за `QuestionRepository`** | 8.55 МБ и 8866 вопросов — цифра, померенная по файлу; шарды решают проблему, которую на телефоне ещё не замеряли. За интерфейсом переход на шарды — замена реализации, а не переделка режима. Честная цена: на web у `compute()` нет изолята, декод подвесит UI, и превью тянет 8.5 МБ |
| ассет в git | **не коммитить, генерировать шагом сборки** | ассет детерминированно выводится из `gq_clean.json`, который уже в git; T11 ещё перегонит корпус не раз, и каждая перегонка навсегда клала бы в историю новый блоб на 8.5 МБ. Цена — один шаг в CI и одна команда после клона, и она закрыта тем, что забытая генерация роняет `flutter test`, а не даёт пустой экран |
| кто генерирует | **отдельный `scripts/build_app_assets.py`** | санитайзер остаётся чистой функцией над дампами и не лезет в `app/` — в том числе потому, что `data/` и `app/` сейчас в работе у разных сессий |
| отбор вопросов | **случайно без повторов + один созревший слот из пяти** | свёртка `dueQuestions` уже написана в T10 и иначе остаётся невостребованной. Раунд целиком из созревших отвергнут: это зубрёжка, а Классика по концепту — поток новых |
| когда пишется событие | **после каждого вопроса** | append-only журнал ровно для этого и заведён; батч в конце раунда теряет всё при выходе из приложения на середине |

##### Зависимости

Требует **T2a** (цикл) и **T11** (чистый дамп — есть, `gq_clean.json` в рабочем состоянии). Бинго-корпус не нужен вовсе: Классика идёт по gq. **Блокирует контрольную точку фазы 1.** Новых пакетов нет.

##### Критерий успеха

- раунд из пяти вопросов проходится на телефоне end-to-end, в `journal.jsonl` пять событий с одним `roundId`, переживших перезапуск приложения;
- **красный→зелёный:** тест, грузящий ассет через `rootBundle`, падает при несобранном ассете и проходит после генерации; тест отбора падает на реализации без учёта виденных;
- **замер, который надо записать сюда фактическим числом:** сколько миллисекунд занимает `loadAll()` на телефоне. Больше секунды — заводим шарды отдельной задачей, меньше — вопрос закрыт. **Снято 30.08.2026: на телефоне 871 мс** (на маке 164 мс). Меньше секунды — вопрос шардов закрыт, но запас тонкий, и на более слабом телефоне порог перейдёт. ⚠️ Тот же замер вскрыл баг: повторное открытие давало 890 мс вместо нуля — репозиторий создавался заново на каждом переходе, и его кеш не работал ни разу. Починено, репозитории живут в `_ChgkTrainerAppState`;
- две недели самопроверки по критериям MVP из «Концепта».

#### ✅ T4a — тап «приём недели» + первая карточка

**✅ Закрыта 30.08.2026.** Тесты и `flutter analyze` зелёные. **Проверено на телефоне: карточка урока читается примерно за полминуты, размер годный.** Первая обкатка нашла два бага интерфейса — см. «Что нашла первая обкатка» ниже.
**Постановка проработана 29.08.2026, переигрaна 30.08.2026 по замерам корпуса — см. «Что пришлось переиграть» ниже.**

Едет вместе с Классикой, не после. Одновременно активное угадывание и разметка: сфокусированная на одном приёме, она накапливается быстро, а не размазывается по девятнадцати. **Цена недели — одно объяснение**, разметка не делается отдельной работой.

##### Как сейчас

Разметки приёмов ноль, автоматически она не берётся (см. «Авторазметка приёмов не работает»). Поле `tehnikaGuess` в `AnswerEvent` есть и никем не пишется. Свёртки «какая сейчас неделя» в `projections.dart` нет.

##### Шаги

1. **`app/assets/tehniki.json`** — массив приёмов: `{id, title, explain, examples: [{questionId, why}]}`. `explain` — 1-2 предложения своими словами, «как выглядит в тексте». `examples` — 3-5 реальных вопросов корпуса, разобранных **своими словами**: таксономия Савченкова употребима как терминология, но абзацы и примеры из книги копировать нельзя (см. T4b). Первый приём — **«замена»**: самый частый в корпусе, контрольные примеры под него ищутся грепом за минуты. Файл собирается руками и **коммитится** — это авторский текст, а не производная от дампа.
   → проверка: у каждого `example.questionId` есть соответствие в `questions.json`; тест это проверяет, иначе опечатка в id тихо превратит контрольный вопрос в обычный.

2. **`weekIndex(List<JournalEvent> events, DateTime now)`** в `projections.dart` — целая часть от (сегодня − самый ранний `day` в журнале) ÷ 7, нумерация с нуля. Пустой журнал → 0. Активный приём — `tehniki[min(weekIndex, tehniki.length - 1)]`.
   → проверка: границы включительно — день 6 → неделя 0, день 7 → неделя 1; журнал пуст → 0 без исключения.

3. **Карточка урока** — показывается автоматически при первом за неделю входе в Классику (в журнале нет ни одного `answer` с текущим `weekIndex`) и всегда доступна кнопкой с главного экрана. Читается за полминуты: название, объяснение, разобранные примеры.
   → проверка: виджет-тест — на пустом журнале карточка показана до первого вопроса; после первого ответа в ту же неделю повторный вход её не показывает.

4. **Тап после раскрытия** — на каждом ~3-м вопросе: «здесь был приём недели?» да/нет → `tehnikaGuess`. Отбор вопросов под тап **детерминированный**: `fnv1a(questionId) % 3 == 0`, не случайный — иначе тест на «тап показан» невоспроизводим, а один и тот же вопрос в повторе ведёт себя по-разному.
   → проверка: на фиксированном наборе id тап показан ровно на ожидаемых; `tehnikaGuess` доезжает до события, а `null` остаётся у вопросов без тапа.

5. **Обратная связь — по контрольным вопросам урока.** Попался вопрос из `examples` активного приёма → показывается вердикт «да/нет, вот почему» с текстом `why`. Любой другой вопрос → «записал», без вердикта: эталона нет и притворяться, что есть, нельзя.
   → проверка: контрольный вопрос даёт вердикт и разбор; неконтрольный — только подтверждение записи, и при этом `tehnikaGuess` всё равно записан.

##### Что пришлось переиграть — 30.08.2026, при реализации

Три замера по `gq_clean.json` показали, что постановка не работает в трёх местах. Записано подробно, потому что ошибка тут концептуальная, а не техническая.

**1. «Замена» первым приёмом не годится — приём объявлен в самом тексте.** Замен в корпусе **2864 из 8866 (32%)**: 703 вопроса пишут «мы заменили» словами, ещё 2161 используют КАПС по конвенции оформления. Тап «здесь был приём недели?» превращается в чтение первой строки, а разметка, ради которой тап и придуман, снимается регуляркой за минуту. Раздел «Авторазметка приёмов не работает» это наполовину и предсказывал («66% — техническое "мы заменили X на Y", конвенция оформления, а не приём рассуждения»), но вывод не был доведён до этой карточки.

**2. Обратная связь по постановке не наступила бы ни разу.** Тап на каждом ~3-м вопросе, контрольных примеров 3–5 на весь корпус: за две недели это ~23 тапа, вероятность попасть на один из пяти — доли процента. Игрок всю фазу видел бы только «записал».

**3. Редкий приём даёт тап, ответ на который всегда «нет».** Замерены кандидаты: «перевод для ответа» — 132, «подсчёт сущностей» — 22, «аналогия» — 16. При частоте 1.5% почти каждый тап отрицательный: ни тренировки, ни разметки. То есть частый приём тривиален, редкий бессмыслен — это дефект механики, а не выбора приёма.

**Что сделано вместо.** Замысел («угадывание и разметка одновременно, цена недели — одно объяснение») сохранён сменой источника эталона:

- первый приём — **«перевод для ответа»**: из текста вопроса не виден (утечка 8%), решается только переводом;
- эталон строится **регуляркой по комментарию** с упором на точность, а не полноту — **91 вопрос**. Оговорка, которая снимает противоречие с «авторазметка не работает»: та не работает как *полная* разметка, здесь же нужна высокая точность при любой полноте. Это разные требования;
- **вердикт выносится только при «эталон говорит да»**. Отсутствие вопроса в эталоне не доказывает отсутствия приёма, поэтому на «эталон не нашёл» показывается честное «записал», а не «приёма не было»;
- **один слот раунда — эталонный вопрос приёма недели.** 91 на 8866 случайно не попадается неделями; без слота вердикт не показался бы ни разу;
- тап показывается на эталонных **всегда** и на каждом третьем из остальных. Только на эталонных нельзя: тогда сам факт тапа означал бы «да».

##### Что нашла первая обкатка на телефоне — 30.08.2026

Два бага, оба про интерфейс, оба поправлены:

- **тап приёма нельзя было переменить.** Ответ фиксировался первым же касанием сегмента, и промах пальцем запирал в случайной догадке — оставалось только идти дальше. Теперь два шага: выбор меняется свободно, кнопка **«Ответить»** фиксирует и показывает разбор. После неё решение заперто — и это осознанно: переписать догадку, уже увидев вердикт, значит сделать разметку липовой. Тот же двухшаговый рисунок, что у самооценки взял/почти/не взял, — раньше фаза приёма была единственной, где мисклик был необратим;
- **карточка урока открывалась дважды подряд.** Она скрывается только после первого *ответа* за неделю, а чтение с главного экрана нигде не отмечалось. Заведён `TehnikaCardSeen` — флаг на запуск приложения, прокинутый сверху. Не событие в журнале: заводить тип события ради «прочитал карточку» — расширение схемы T10 под нужду, которая исчезает после первого же ответа. Цена — после перезапуска приложения карточка всплывёт ещё раз, если играть игрок так и не начал.

##### Развилки — закрыты 29.08.2026, пересмотрены 30.08.2026

| развилка | выбор | почему |
|---|---|---|
| ~~обратная связь только на 3–5 контрольных~~ | **вердикт на всём эталоне приёма (91 вопрос), «записал» вне его** | контрольных примеров слишком мало, чтобы тап на них попал: вердикт не показался бы ни разу за две недели. Примеры остались в карточке урока — как «как выглядит в тексте», а не как эталон |
| ~~первый приём «замена»~~ | **«перевод для ответа»** | замена объявлена в тексте у всех 2864 вхождений — угадывать нечего, разметка снимается регуляркой |
| как строится эталон | **регулярка по `comment`, поле `detect` в `tehniki.json`** | правило поиска — часть описания приёма, живёт рядом с объяснением. Исполняет его **скрипт сборки**: приложение регулярок из ассета не запускает, в `questions.json` едет готовое поле `tehniki` |
| какая сейчас неделя | **по собственному стажу из журнала** | сервер не нужен, таймзоны не врут (`day` уже локальная дата), первый запуск в четверг не даёт огрызок в четыре дня вместо первого урока |
| частота тапа | **детерминированно по хешу `questionId` + всегда на эталонных** | случайный отбор делает тест невоспроизводимым, а поведение приложения — разным при повторной встрече того же вопроса. Хеш — `fnv1a`, а не `String.hashCode`: последний не обязан быть стабильным между запусками |
| где живёт контент приёмов | **`app/assets/tehniki.json`, коммитится** | это авторский текст, а не производная от дампа: генерировать его нечем, и в git он должен быть. ⚠️ `.gitignore` игнорирует `app/assets/*.json` — для него заведено исключение |

##### Зависимости

Требует **T2a** (`config.tehnika` в цикле) и **T2b** (`questions.json`, в котором ищутся контрольные id). Разметка, которую копит этот тап, — топливо для **T4b** и для подсказки, которой в фазе 1 нет.

##### Критерий успеха

- за две недели самопроверки в журнале накопились `tehnikaGuess` — то есть тап реально попадается, а не оказался незамеченным;
- **красный→зелёный:** тест границ `weekIndex` (день 6 против дня 7) падает на реализации с делением по календарным неделям; тест «id из `examples` существует в корпусе» падает на опечатке в id — **проверено мутацией 30.08.2026**, как и гард «ассет приёмов объявлен в `pubspec.yaml`»;
- ручная проверка одна: карточка урока читается за полминуты и объясняет приём без обращения к книге.

#### ✅ T2c — чем обеспечена контрольная точка фазы 1

**✅ Закрыта 30.08.2026.** Шаг 1 — джоба `apk` зелёная с первого прогона, приложение поставлено и отыграно. Шаг 2 — `lib/screens/debug_journal_screen.dart`, вход долгим тапом по заголовку. Оба числа с экрана сняты: журнал пережил перезапуск, `loadAll()` — 871 мс.
**Постановка проработана 29.08.2026.**

Карточка существует ровно затем, чтобы контрольная точка фазы 1 состоялась. Сейчас она не обеспечена ничем: пройти две недели негде и сверить результат нечем.

**Два шага, две разные точки в порядке работ.** Шаг 1 (APK) делается **до T2a**, первой работой фазы: обкатка идёт на телефоне с самого начала, и сборочный канал проверяется на скелете, где ломаться нечему. Шаг 2 (экран) — после T2b, раньше в журнале нечему накапливаться.

##### Как сейчас

`.github/workflows/build.yml` собирает **только web**; `app/android/` в `.gitignore` и генерируется на бегу через `flutter create`. На web журнал — `MemoryEventLog`, то есть прогресс не переживает перезагрузку и ни streak, ни возврат проваленных там не работают в принципе. На телефоне журнал живёт в песочнице приложения (`getApplicationDocumentsDirectory`), достать его без adb нельзя. Экрана метрик нет: T9 стоит в фазе 3, и это правильно — но тогда критерии MVP («возвращаюсь ≥5 дней из 7», «растёт доля взял») мерить нечем.

##### Шаги

1. **Джоба `apk` в `build.yml`** — рядом с web: `flutter create --platforms=android --org dev.chgktrainer --project-name chgk_trainer .` → `flutter build apk --debug` → `upload-artifact`. Ассета на этот момент ещё нет и `pubspec.yaml` его не объявляет — шаг генерации в обе джобы добавляет T2b, вместе с самим ассетом; сделать наоборот нельзя: объявленный, но несобранный ассет роняет сборку. Debug-подписи достаточно: приложение ставится с «неизвестного источника» на свой телефон, релизная подпись нужна только под OTA, которого в фазе 1 нет.
   → проверка: артефакт скачивается, ставится, приложение стартует и создаёт `journal.jsonl`, переживающий перезапуск. ✅ Сборка: джоба `apk` зелёная с первого прогона; шаблонному Gradle понадобился явный `setup-java` c JDK 17 — на дефолт раннера не закладываемся.

   **Заодно, раз файл всё равно открыт — сделано 30.08.2026:** прогон предупреждал, что экшены целятся в Node.js 20 и принудительно запускаются на Node 24. Подняты все пять: `checkout@v7`, `upload-artifact@v7`, `upload-pages-artifact@v5`, `deploy-pages@v5`, `setup-java@v6`. ⚠️ Совет «поднять до `@v5`», стоявший здесь раньше, был неверен: `upload-artifact@v5` — всё ещё `using: node20`, Node 24 появляется только с v6. Версию мажора нельзя угадывать по номеру — проверяется полем `runs.using` в `action.yml` соответствующего тега. Второй источник того же предупреждения был спрятан: `upload-pages-artifact@v3` — composite, внутри он звал `upload-artifact@v4`. Риск от подъёма ровно один: с `upload-pages-artifact@v4` в артефакт Pages не попадают файлы с точки в начале имени; в `build/web` таких нет.

2. **`lib/screens/debug_journal_screen.dart`** — сырые числа из уже написанных свёрток: `currentStreak`, `takenRate(window: 50)`, всего ответов, `dueQuestions().length`, дата первого события и **`skippedLines`**. Вход — долгий тап по заголовку главного экрана, а не пункт меню: это отладка, а не функция приложения.
   → проверка: виджет-тест поверх `MemoryEventLog` — на трёх событиях показаны ожидаемые числа; журнал с битой строкой показывает `skippedLines > 0`, а не прячет её.

   **Сделано 30.08.2026.** Показывает: событий, ответов, streak, долю взятых за 50, сколько ждёт возврата, неделю стажа, дату первого события, `skippedLines` и — сверх постановки — **число вопросов в ассете и миллисекунды `loadAll()`**. Замер добавлен потому, что это критерий успеха T2b, и снять его больше негде: на глаз получается только «достаточно быстро». Значения больше секунды и любое ненулевое число битых строк подсвечиваются красным.

   ⚠️ **Грабли, всплывшие тут же:** `JournalScope.of(context)` — это `dependOnInheritedWidgetOfExactType`, и звать его из `initState` нельзя. В `classic_screen.dart` та же ошибка была с самого начала и не выстреливала лишь потому, что журнал читался после первого `await`. Оба экрана переведены на `didChangeDependencies`.

##### Развилки — закрыты 29.08.2026

| развилка | выбор | почему |
|---|---|---|
| под какие архитектуры собирать APK | **только `android-arm64`, с 30.08.2026** | сборка под все три давала 161 МБ на каждую перекачку против ~60; целимся в Android-телефон, остальные ABI не нужны никому |
| как приложение попадает на телефон | **джоба в CI, APK артефактом** | сборка перестаёт зависеть от локального Android SDK, который на этой машине не проверен ни разу; выяснять его состояние в момент, когда приложение готово и хочется его поставить, — худший из возможных моментов |
| где смотреть накопленное | **экран в самом приложении** | журнал лежит в песочнице, и выгрузка наружу — это новый пакет плюс два шага руками каждый раз, когда захотелось посмотреть одну цифру |
| не подменяет ли это T9 | **нет: сырые числа, вход спрятан** | T9 — это профиль и карта слабых мест с дизайном; здесь четыре строки текста для сверки с критериями, которые выбрасываются или перерастают в T9 по её ходу |

##### Зависимости

Шаг 1 не требует **ничего** и идёт первым в фазе — он собирает то, что уже лежит в репозитории. Шаг 2 требует **T2b**: до неё в журнале нечему накапливаться. Ничего не блокирует, кроме честности самой контрольной точки. Новых пакетов нет.

##### Критерий успеха

- APK из артефакта CI ставится на телефон и запускается — **проверено на скелете, до первой строки цикла**;
- после двух дней игры отладочный экран показывает ненулевой streak и долю взятых, а `journal.jsonl` переживает перезапуск приложения;
- по итогам двух недель в контрольную точку едут **числа**, а не ощущение.

#### 🔴 T17 — заголовок: «ЧГК-тренажёр» вместо «Панда будет?»

**Источник:** первый прогон APK на телефоне, 30.08.2026.

`app/lib/screens/home_screen.dart:50` — `AppBar(title: Text('ЧГК-тренажёр'))`, заглушка из скелета T2; название закрыто ещё в T7. Заодно проверить два соседних места, где висит то же рабочее имя: `pubspec.yaml:2` (`description`) и подпись под иконкой на телефоне — `android:label` генерируется `flutter create` из `--project-name chgk_trainer`, то есть на домашнем экране приложение тоже подписано не так.

→ проверка: на главном экране и под иконкой — «Панда будет?».

**Сделано в коде 30.08.2026** — четыре места, рабочее имя не осталось нигде на виду:

- `app/lib/screens/home_screen.dart` — заголовок шапки; долгий тап в отладку сохранён;
- `app/pubspec.yaml` — `description`;
- `.github/workflows/build.yml`, шаг **Patch AndroidManifest** — `android:label` правится сразу после `flutter create` (папка `android/` в git не лежит, править нечего кроме как в CI). Замена проверена на настоящем выводе `flutter create --project-name chgk_trainer`: было `android:label="chgk_trainer"`. Шаг падает явно, если `android:label` не найден, — иначе смена шаблона Flutter тихо вернула бы `chgk_trainer` на домашний экран;
- там же — заголовок GitHub-релиза `auto`.

Регрессия закрыта виджет-тестом `app/test/home_screen_test.dart` («в шапке — название приложения, а не рабочее имя»). `flutter analyze` чист, 104 теста зелёные.

✅ **Запушено 30.08.2026**, гейт проверен командой: `git merge-base --is-ancestor 3d384e1 origin/main` — да. Прогон CI зелёный целиком, шаг `Patch AndroidManifest` прошёл; релиз `auto` называется «Панда будет? 0.1.0 (build 13)», манифест по боевому адресу отдаёт `app_build: 13`.

⏳ **К закрытию остался один пункт:** обновиться кнопкой на телефоне и посмотреть подпись под иконкой — `android:label` виден только на установленном APK.

#### 🟡 T18 — блокнот версий во время минуты сбивает

**Источник:** тот же прогон, 30.08.2026. Формулировка с телефона: «сбивает, что во время минуты есть поле для ввода версии, которое обнуляется, когда жмёшь „готов отвечать“ — возможно, его стоит убрать и писать только финальный ответ».

**Что там на самом деле** (`app/lib/cycle/question_cycle.dart:147–174`): блокнот принимает версию только по Enter (`onSubmitted`), список `drafts` живёт в контроллере, в журнал не едет и в поле ответа не переносится. Рвётся дважды: недонабранная строка пропадает молча, а сохранённые версии никак не помогают заполнить ответ в окне записи. Снаружи это выглядит как «поле обнулилось» — то есть работа за минуту исчезла.

**Развилка — решить до кода:**

| вариант | что даёт | чем платим |
|---|---|---|
| **убрать блокнот, версия пишется только в окне записи** | ровно то, что предложено; цикл возвращается к турнирной конвенции — версия сдаётся один раз | во время минуты некуда сложить промежуточные догадки; на живой игре это делает капитан на бумаге |
| оставить, но не терять: недонабранный текст уезжает в `drafts` по «Готов отвечать», последняя версия предзаполняет поле ответа | мысль не пропадает | больше состояния и кода в самом горячем месте цикла |

Рекомендация — первый: он снимает жалобу целиком и уменьшает код, а блокнот вернуть дешевле, чем чинить полумеру, если на обкатке выяснится, что его не хватает.

→ проверка: виджет-тест — в фазе минуты поля ввода нет; текст, набранный в окне записи, доезжает в событие журнала.

#### 🟡 T3 — сетка «Бинго»

**Цикл:** вопрос → минута → своя версия → **и только теперь** сетка: «к какой из 9 клеток?» + обязательный вариант «ни к одной» → раскрытие ответа и клише → самооценка.

Выбор клетки — второе независимое суждение после того, как игрок уже связал себя версией.

**Состояния клетки:** пустая · закрашена (узнал клише) · золотая (узнал и взял вопрос). Линия считается по закрашенным любого цвета; золото — флекс, не гейт.

**Топливо — из двух корпусов:**

- вопросы по темам сетки (~2/3 раунда) — из конечного корпуса бинго;
- отвлекающие (~1/3) — **из gq-корпуса**, который безграничен.

Так «ни к одной» верна примерно в трети случаев — осмысленно, но не доминирует, и совпадает с живой игрой, где клише в меньшинстве. Дефицитный корпус бинго не тратится на отвлекающие вообще.

**Пограничный случай — фича:** если отвлекающий gq-вопрос окажется клише и игрок ткнёт в клетку, это не ошибка, а предложение разметки в журнал.

**Сборка сетки:** 6 неосвоенных / 3 освоенных, перемешаны. Освоенность — свёртка журнала (узнавал ли надёжно), не отдельное хранилище. Дополнительное условие: тема попадает в клетку, только если у неё остался непоказанный вопрос.

**Цена ошибки** отдельной механики не требует: раунд из 5 вопросов, неверная догадка тратит одну из пяти попыток закрыть линию.

**Исключить из пула сеток:** метатемы «МетаЧГК», «Сленг ЧГК», «Замена по цитате», «Переворот раздатки» — это темы-приёмы, а не темы-ответы, в вопросе «к какой из девяти» они несопоставимы с остальными.

#### 🟡 T14 — справочник

Проза статей уже есть, писать почти нечего. Формат вики-статьи — ровно нужный: **факт** (эрудиция) + **как обыгрывают** (узнавание).

**Три точки входа, все по ходу игры** — справочник это награда за клетку, а не вкладка в меню. Холодное чтение справочной прозы не делает никто; чтение через пять секунд после промаха на этом клише делают все.

1. под раскрытым ответом — свёрнутая карточка «Клише: Ковентри», тап разворачивает статью;
2. тап по закрашенной клетке сетки;
3. экран «Справочник» — индекс кампании с состояниями `узнано / встречалось / не встречалось`, он же оглавление.

**Накопитель обеих осей:** ~330 клише готовы из статей, ~19 приёмов дописываются по одному в неделю карточками T4a. Через 19 недель вторая половина книги написана сама.

**Плюс своя заметка на клише** — место, куда пишешь понятое после промаха. Один `TextField`, пишется в журнал.

**Спойлеры не блокируем:** соло-тренажёр на честной самооценке, обманывать некого. Достаточно того, что узнавание в дикой природе весит больше, чем угадывание в сетке, — первое статьёй не подделаешь.

#### 🟡 T4b — полный цикл «приёма недели»

- воскресный гаунтлет: накопленные за неделю вопросы, «где здесь приём?» без подсказки;
- тап по фразе-триггеру в тексте после раскрытия — помечаем не только категорию, но и конкретный маркер;
- «проверка вслепую» — время от времени вопрос без подсказки приёма вообще, мостик обратно в Классику;
- личная карта слабых мест — прямая визуализация «чаще всего путаешься в приёме X».

**Планировщик:** вместо Leitner по 19 приёмам (требует, чтобы все 19 уже существовали) — простое правило: если «не увидел приём» доминирует за неделю, приём недели не двигается дальше.

**Таксономия (~19):** 12 подтипов ложного хода (слово / название / человек / животное / произведение как другая сущность, вымысел и метафора как реальность, замена, часть слова как слово, ненужная информация) плюс аналогия, тавтология, перевод для ответа, учесть имя, аббревиатура, подсчёт сущностей, палиндром.

**Начинать не с девятнадцати, а с 5-6.** «Приём недели» и так открывает по одному; 19 объяснений до первого запуска — гарантированная смерть задачи. Брать те, что встречаются чаще: замена, аналогия, перевод для ответа, аббревиатура, подсчёт.

⚠️ **Правовая оговорка:** таксономия Савченкова из коммерческой книги (ISBN 978-5-4491-2525-5). Названия приёмов и методику категорий использовать можно — общеупотребимая терминология ЧГК-сообщества. Копировать абзацы и примеры из книги нельзя: свой банк примеров, своя разметка реальных вопросов.

#### 🟢 T12 — «Вопрос дня»

Крючок NYT Games — не XP, а один конечный общий объект на сегодня: сегодняшний вопрос кончается, и в этом всё дело. Контент-цена нулевая.

Один и тот же вопрос для всех в сутки — детерминированный выбор по дате, сид от `YYYY-MM-DD`, сервер не нужен. Реплика панды, локальная история пройденных дней, одна попытка.

**Задел:** отсюда естественно вырастает шеринг результата без спойлеров по образцу Wordle — но в MVP не делаем, см. анти-бэклог.

#### 🟢 T9 — профиль и метрики

Единый профиль сверху + drill-down в нативную метрику каждой оси снизу.

- **наигранность** — сколько вопросов пройдено, доля «взял» на скользящем окне;
- **кампания** — X / ~330 клише узнано, и отдельно сколько узнано **в дикой природе**, а не в сетке;
- **приёмы** — X / 19 освоено, карта слабых мест;
- **streak** — дни подряд, когда заходил.

Всё — свёртка журнала T10, параллельных хранилищ нет.

⚠️ **XP и уровни сняты.** Начисление «за верный ответ» опиралось на бинарность, которой больше нет, и требовало придумывать веса для «почти» и «взял с подсказкой» — проектирование ради числа, которое ничего не решает. Роль ритуала играют «Вопрос дня» и конечная кампания.

#### 🟢 T8 — визуальный тон и панда

**Персонаж:** противоречивый сокомандник, не мотиватор-клакер уровня совы Duolingo. Любящий стёб между своими, как в реальных ЧГК-командах.

**Форма мягкая, характер контрастный** — весь эффект на несоответствии:

- постоянная деталь лица: приподнятая бровь / полуприкрытые глаза как «resting judgmental face», не ситуативная эмоция;
- библиотека из 6-8 узнаваемых поз важнее новых форм тела: скрещенные лапы (самодовольство), медленные хлопки (ехидная похвала), фейспалм, поднятая лапа «стоп»;
- возможен фирменный атрибут (бамбук как ленивая зубочистка) — на этапе арта, жёстко не зафиксировано.

**Голос — отдельный визуальный слой:** комикс-пузырь, отличный от системных тостов, чтобы было ясно — говорит ОН, а не интерфейс. Пауза перед репликой — комедийный тайминг почти бесплатно.

**Бюджет искренности:** не чаще 1 реплики из ~30. Объём банка — ~10 строк × 8 моментов ≈ 80 до заметных повторов.

✅ **Банк реплик сделан 30.08.2026.** Черновик `data/panda_lines_draft.md` вычитан без правок, скриптом разобран в `app/assets/panda_lines.json` — 9 моментов, 85 строк, редкие искренние лежат отдельным пулом.

Механика — `app/lib/panda/panda_voice.dart`, все три правила из карточки в одном месте: молчание примерно в половине моментов, редкая реплика раз из тридцати, никогда не повторяться подряд. Голос раздаётся через `PandaScope`, а не через конструкторы: реплика нужна в глубине цикла вопроса, и тянуть её пятью параметрами значит менять пять сигнатур ради одной строки. `PandaScope.maybeOf` возвращает `null` — экран без голоса просто молчит, поэтому ни один существующий тест не пришлось трогать.

Пузырь — `app/lib/widgets/panda_bubble.dart`: светлый с тёмным текстом и хвостиком в обеих темах, чтобы его нельзя было спутать с карточками и тостами. Контур рисуется одним путём, а не плашкой с приклеенным треугольником — иначе обводка идёт по стыку. Появляется через паузу в 700 мс: сначала игрок видит результат, потом панда его комментирует.

⚠️ **Подключены только четыре момента из девяти** — три вердикта и итог раунда. `streak.*`, `weakmap.show`, `loading`, `empty` написаны и лежат в ассете, но экранов под них нет (T9, T4b): вешать их сейчас значило бы писать мёртвый код. Тест ассета следит, чтобы в подключённых моментах не завелось подстановки `{n}` — такая строка молча исчезла бы с экрана.

✅ **Позы подключены 30.08.2026.** Одиннадцать поз лежат в `app/assets/panda/`, четыре подключены к работающим моментам: три вердикта и итог раунда. Маппинг — `app/lib/panda/panda_poses.dart`, отдельно от `PandaMoments`, потому что связь не один-к-одному.

Панда показывается **и когда молчит**: голос молчит примерно в половине моментов, и если бы вместе с репликой пропадала картинка, экран прыгал бы на каждом вердикте, а молчание читалось бы как «панды тут нет». Она есть — просто ничего не сказала.

Редкая искренняя реплика идёт со своим лицом. Ради этого `lineFor` стал обёрткой над новым `speakFor`, возвращающим `PandaSpeech` с признаком редкости: без него единственная поза набора с настоящей улыбкой не показалась бы никогда, а «Вот это было красиво. Правда.» произносилось бы с тем же прищуром, что и подколка. Голос спрашивается один раз в `PandaSays` и передаётся в пузырь готовым — два запроса дали бы разные ответы, и поза разошлась бы с репликой.

**Нарезка воспроизводима:** `scripts/cut_panda_sheet.py`. Масштаб считается по ширине головы, а не по габариту кадра (поза с вытянутой лапой шире прочих, и подгонка по рамке сделала бы её панду мельче), кроп — общий на весь набор, иначе позы разъезжаются по вертикали. Ассеты — 420 px по высоте с квантованной палитрой, 817 КБ на одиннадцать файлов вместо 2,6 МБ труколором.

⚠️ **Исходники — не оригиналы.** Позы взяты из картинок, прошедших через чат: альфа-канал потерян и восстановлен по цвету, поэтому края жёсткие, без полупрозрачности. На экране в 116 точек не видно, но при получении оригинальных файлов от генератора набор стоит перенарезать тем же скриптом.

⚠️ **Две позы не переделаны.** `clap` — разведённые лапы, читается как пожимание плечами, а не медленные хлопки; `waiting` — подпёртая морда без зевка, ближе к грусти, чем к скуке. Промт на правку написан, генерация не сделана.

⚠️ **Тон не проверен на живой игре.** Реплики читались на бумаге; как «Молчу.» звучит через полминуты после того, как не взял вопрос, покажет только обкатка.

**Где звучит:** обратная связь после ответа (T2a), карта слабых мест (T4b), стрик и его разрыв (T9), пустые экраны и загрузка.

**Палитра и типографика:** чёрно-белая база панды + тёплый золотой акцент (отсылка к совам-трофеям) + тёмно-зелёный как поддерживающий (сукно стола). Тёмная тема по умолчанию (ночная культура живых квизов), светлая — альтернатива.

✅ **Тема сделана 30.08.2026** — `app/lib/app_theme.dart` переписан: явные `ColorScheme` вместо `fromSeed` (сид размывает именно те три цвета, ради которых палитра и заводилась), тёмная по умолчанию, светлая собрана. Nunito лежит в `app/assets/fonts/` тремя статическими начертаниями; происхождение и почему не вариативные — `app/assets/fonts/README.md`.

⚠️ **Сериф для текста вопроса снят с плана.** Ставился PT Serif, на живом экране резал глаз. Решение: одно округлое семейство на всё, вопрос отличается кеглем (19) и межстрочным (1.55), а не рисунком букв — тот же ход, что у Duolingo. Заодно −700 КБ.

⚠️ **Второй заход по контрасту.** Первая версия была почти белым по почти чёрному и «била в глаз» — светлые буквы на чёрном раздуваются по краям. Текст приглушён до `#E2DED4`, фон поднят до `#121615`. Отдельно всплыло «чёрное на чёрном»: четыре карточки главного экрана не отличались от фона и друг от друга. Лечится не цветом текста, а контуром карточки и золотыми иконками — золоту нужно место, где оно видно, иначе акцента в приложении нет вовсе.

✅ **Шкала кеглей — 13 · 15 · 17 · 20 · 24 · 30.** Размеры прыгали, потому что часть текста брала стиль из темы, а часть — зашитые числа (18, 20, 22). Введены шесть ступеней в `TextTheme`, из экранов убраны все `fontSize`. Правило на будущее: **в экранах нет `fontSize`, есть слот шкалы** — иначе разнобой возвращается с первым же новым экраном.

✅ **Золото металлическое, а не плоское.** Плоская заливка выглядела жёлтым пластиком. Градиент кладётся через `ButtonStyle.backgroundBuilder` в теме — форма, обрезка и рябь остаются родные, ни один экран не тронут. Блик вертикальный и узкий: диагональ на широкой кнопке растягивалась в засвет левой половины, а вертикаль не зависит от ширины.

✅ **Название приложения** — 30 кегль по центру шапки, набрано тем же золотом через `ShaderMask` (`PandaWordmark`). Единственное место, где блеск лежит на самом тексте: это имя, а не интерфейс. Долгий тап в отладку сохранён.

✅ **Карточка урока перебрана.** Одной шкалы ей не хватило — «убого» было от вёрстки: шапка экрана и первая строка тела обе говорили «Приём недели»; зелёный блок висел без подписи, и было не понять, объяснение это или предупреждение; три примера шли сплошняком и сливались в стену. Стало: заголовок только в теле, у блока подпись «Как заметить», каждый пример — отдельная карточка с меткой «Ответ» и разделителем перед разбором.

⏳ **Переключателя тем нет** — светлая собрана, но не выбирается. Появится вместе с экраном настроек; сейчас заводить его не под что.

---

#### ✅ T19 — самообновление APK (OTA)

**Проблема:** APK ставится на телефон вручную — скачать артефакт из CI, перекинуть, установить. После каждой правки заново; обкатка тормозится не разработкой, а перекладыванием файла.

**Решение — по образцу `~/speech-rehab` (Tier B), код переиспользован почти дословно.** CI после сборки публикует подписанный APK в GitHub Release вместе с `app_manifest.json`; приложение читает манифест со стабильного адреса `releases/latest/download/app_manifest.json`, сравнивает `app_build` со своим, качает APK, сверяет sha256 и открывает системный установщик. Кнопка — в шапке главного экрана.

**Что сделано (код, 30.08.2026):**

- `app/lib/data/ota_config.dart` — адрес манифеста и таймауты;
- `app/lib/data/app_updater.dart` + `_io` / `_stub` — проверка и установка, на web заглушка;
- `app/lib/widgets/update_button.dart` — кнопка и диалоги; 4 виджет-теста на подставном апдейтере;
- `scripts/build_app_manifest.py`, `scripts/inject_android_signing.py` — манифест и впрыск релизной подписи;
- `.github/workflows/build.yml` — релизная подпись, `INTERNET` + `REQUEST_INSTALL_PACKAGES`, `--release`, публикация релиза.

**Развилки, решённые здесь:**

| развилка | решение | почему |
|---|---|---|
| ключ подписи | **переиспользуем `speech-rehab-release.jks`** | ключ уже есть; applicationId разные, конфликта нет. Цена — один keystore на два приложения: потеряется файл, потеряются оба |
| откуда build-номер | **номер прогона CI** (`--build-number=${{ github.run_number }}`) | монотонно растёт сам; бампать `pubspec` руками перед каждой сборкой — ровно тот шаг, который забывается, и обновление молча «не находится» |
| сколько релизов держать | **один тег `auto`, релиз пересоздаётся** | иначе каждый push плодит релиз-однодневку; истории сборок здесь никто не читает |
| debug или release | **release** | debug-ключ на раннере эфемерный: каждая сборка — «чужое» приложение, установка поверх невозможна. Это свойство Android, обойти нечем |

**Что осталось (не код):**

1. Завести в репозитории четыре секрета: `ANDROID_KEYSTORE_BASE64`, `ANDROID_KEYSTORE_PASSWORD`, `ANDROID_KEY_PASSWORD`, `ANDROID_KEY_ALIAS`. Без них apk-джоба падает явно на первом шаге.
2. **Разовый разрыв:** установленный debug-APK удалить с телефона и поставить релизный руками — подписи разные, поверх не встанет. Дальше только кнопкой.

**Проверка закрытия — состояние на 30.08.2026:**

- ✅ коммит `8739a56` в `origin/main`, прогон CI зелёный целиком (web 2m29s, apk 5m16s, pages 10s);
- ✅ релиз `auto` опубликован: `ЧГК-тренажёр 0.1.0 (build 11)`, оба ассета на месте;
- ✅ манифест по боевому адресу `releases/latest/download/app_manifest.json` отдаёт `app_build: 11`;
- ✅ APK скачан по `apk_url`, sha256 сошёлся с манифестом побайтово; размер 20.7 МБ против ~60 у прежней debug-сборки;
- ✅ APK подписан по схеме v2 (блок подписи на месте) — впрыск релизной подписи сработал;
- ✅ релизный APK поставлен на телефон вручную (разовый разрыв пройден), кнопка ответила «Обновлений нет, build 11»;
- ✅ **сквозной прогон:** прогон вручную собрал build 12, манифест обновился, кнопка нашла сборку, скачала и поставила — обновление доехало до телефона без перекладывания файла.

**Закрыта 30.08.2026.** Гейт проверен командой: `8739a56` и `176e8b1` — предки `origin/main`.

**Что помнить дальше:**

- сборка запускается только на изменения в `app/`, `data/`, `scripts/` и в самом `build.yml`. Правка одного бэклога новую сборку не даёт — если нужна, дёргать вручную (Actions → build → Run workflow);
- keystore один на два приложения (`~/speech-rehab-release.jks`). Копия файла вне этой машины — незакрытый риск: потеря = невозможность обновлять и chgk, и speech-rehab.

⚠️ **Побочный эффект:** релиз пересобирается на каждый push, попадающий под `paths:` (включая `data/**`) — то есть коммит контента тоже выкатывает новую сборку. Приемлемо: сборка та же, меняется только номер.

---

### Контент

#### ✅ T11 — санитайзер дампов

**Закрыта 29.08.2026.** `scripts/sanitize_dumps.py` → `data/gq_clean.json`, `data/bingo_clean.json`, `data/sanitize_report.json`, `data/sanitize_sample.json`. Скрапер `scrape_bingo_wiki.py` починен, вики-дамп пересобран. Тесты — `python3 scripts/tests/test_sanitize.py`.

##### Что получилось

| корпус | было | осталось | отбраковано |
|---|---|---|---|
| gq | 9280 | **8866** | раздатка 264 · дуплет/блиц 136 · медиа 9 · без ответа 4 · пусто 1 |
| bingo | 647 | **599** | медиа 26 · метатемы 16 · раздатка 4 · дуплет 2 |

Тем в бинго-корпусе осталось **132**, из них с тремя и более вопросами — **103**. Сам корпус попутно вырос: пересборка по чистому вики-дампу дала 647 вопросов / 136 тем вместо прежних 635 / 135. Прежняя оценка «595 / 130» была близкой, но невоспроизводимой: правила в ней заданы не были. Теперь числа печатает сам скрипт в `sanitize_report.json`.

Мойибейк в тексте вопросов и в прозе статей — **0** в обоих корпусах (было 140 символов в 44 темах). Оставшиеся 39 символов `` в gq сидят внутри URL в поле `sources` — это битые ссылки на стороне источника, игроку они не показываются, и восстановить их неоткуда.

##### Формат чистого дампа

Каждая запись едет со всеми исходными полями плюс:

```
corpus          gq | bingo
question        очищённый текст (инструкция чтецу срезана, \xa0 схлопнуты)
questionRaw     исходный текст — только если чистка что-то изменила
excluded        null | handout | media | duplet | empty | no_answer | meta_theme
excludedBy      человекочитаемая причина — чтобы разбирать ложные срабатывания
                не грепом заново
acceptVariants  нормализованные допустимые ответы для подсказки автопроверки
```

Ничего не удаляется: отбракованное остаётся в файле с причиной, краулить заново не придётся.

##### Мойибейк: причина была в нашем коде

Задача предполагала чинить `` эвристикой по контексту. Проверка на живом снапшоте `Ab ovo` показала другое: архивная страница — чистый UTF-8, `«»` на месте. `lxml.html.fromstring(r.content)` без указания кодировки разбирал её как latin-1, кириллица превращалась в `Ð‘Ð»Ð°Ð³Ð¾Ð´Ð°Ñ€Ñ`, и ради этого была написана `fix_mojibake` — которая попутно **уничтожала корректные `«»` и неразрывные пробелы**: одиночный `«` (U+00AB) это ран из одного байта, `b'\xab'` как UTF-8 не декодируется, и `errors="replace"` оставлял на его месте ``. Отсюда и `.:-)` — съеденный неразрывный пробел перед смайликом, а вовсе не кавычка.

`fix_mojibake` и `_fix_run` удалены. Разбор идёт через `parse_html()` с явной кодировкой: строгий UTF-8, при `UnicodeDecodeError` — cp1251. Фикстура `scripts/tests/fixtures/ab_ovo.html` держит этот случай красно-зелёным тестом без сети.

Побочно: тот же краулер пойдёт за ~200 статьями индекса в T16 — там баг больше не воспроизведётся.

##### Крауль инкрементальный, а не «всё или ничего»

Первый полный прогон уткнулся ровно в то, от чего защищались: archive.org отдал 53 темы из 139 и ушёл в 503. `save_if_not_worse()` не дал затереть прежний дамп — но прогон «всё или ничего» выбрасывал бы и эти 53.

Поэтому в каждую тему пишется `decoder` — каким кодом она взята. **«Уже скачано» и «скачано правильно» — разные вещи:** тема из старого прогона несёт мойибейк, который сам по себе не чинится. Скрипт по умолчанию идёт только за несвежими, сливает результат в существующий дамп, и неудачный запрос не имеет права затереть уже имеющийся текст. `--all` заставляет перекраулить всё. Плюс общий `polite_get` с растущим отступом (5/15/45 с) и пауза 2 с между темами.

Сошлось за три прохода: **53 → 125 → 137** тем с текстом, мойибейк **140 → 77 → 9 → 0**.

Две темы — `Ной` и `Что у меня в кармане?` — не берутся структурно: в их снапшотах нет блока `mw-content-text`. Это не сетевой сбой, повторы не помогут. На игру не влияет, вопросов из них не было и раньше.

Тем же кодом T16 пойдёт за ~200 статьями индекса и упрётся в ту же стену — вложение окупается дважды.

##### Что вычитка выборки нашла сверх постановки

Ручной шаг 30/30 был в постановке помечен обязательным — и окупился. На первой выборке: **5 отбракованных из 30 были играбельны**, **2 пропущенных требовали раздатки**. Правила исправлены, повторная вычитка сделана на **другой выборке** (сид 777 вместо 20260829): 0 и 0.

1. **`прослушайте` — не медиа.** В ЧГК это обращение к чтецу, а фрагмент приведён тут же текстом. Правило резало 58 играбельных вопросов gq и 13 bingo. Из `RE_MEDIA` убрано; остались только медиа-URL, `[Аудио:`, `видеофрагмент`, `видеовопрос`, `аудиофрагмент`.
2. **`блиц` без нумерации частей — содержание, а не формат.** Отбраковка теперь по маркеру в начале строки **либо** по упоминанию вместе с нумерацией `1. … 2. …`. Ловит `"Анатомический" блиц … 1. … 2. …`, не трогает «команда выиграла блиц на турнире».
3. **Отбраковка смотрит на исходный текст, чистка отдаёт очищенный.** `[Указание организаторам по раздаточному материалу: …]` — сама метка раздатки; срезав инструкцию первой, санитайзер переставал раздатку видеть. Якорное `^Перед вами`, наоборот, проверяется по очищенному — инструкция чтеца могла стоять перед ним.
4. **`Незачёт:` в поле зачёта.** `«Атомный реактор. Незачет: Термоядерный реактор.»` — прежняя версия заводила **неверный** ответ в допустимые варианты, то есть подсказка предлагала бы «взял» за ошибку. Строка обрезается по маркеру; 99 записей несут его. Формулировка «без неверных уточнений» под маркер не попадает и вариант не теряет.
5. **Слэш и кавычки.** Слэш разделяет варианты только с пробелами по обе стороны — иначе `«AC/DC»`, `«5/9 градуса»` и `«1984 1/2»` рассыпались на куски. Нормализация снимает кавычки и пунктуацию **по кругу до неподвижной точки**: у `«"AC/DC".»` кавычка становится хвостовой только после снятия точки.
6. **Ответ `«.»`** нормализуется в пустоту — это отсутствие ответа, а не ответ. Проверка `no_answer` идёт по нормализованному значению.
7. **Квадратные скобки в ответах вообще не разбирались** — а это 528 ответов из 9915, то есть для 5% корпуса подсказка автопроверки молча не работала. `[…]` означает необязательную часть: «[король] Артур» принимается и как «Артур», и как «король Артур», но «король» ответом не является. Круглые скобки значат другое и тоже не однородны: «Уорхол (Энди Уорхол)» — полная форма того же ответа, а «Температуру воздуха (1 градус по Фаренгейту равен 5/9 градуса)» — пояснение, и брать его за ответ нельзя. Содержимое круглых скобок становится вариантом **только когда текст снаружи входит в текст внутри** — это ровно случай развёрнутого имени.

##### Развилки — закрыты 29.08.2026

| развилка | выбор | почему |
|---|---|---|
| куда пишется результат | **отдельные `data/*_clean.json`** | сырой дамп остаётся выводом краулера, санитайзер — чистой функцией. Правило поменялось — перегнал скрипт за полсекунды, а не перекраулил |
| мойибейк | **починить скрапер и перекраулить** | причина оказалась в нашем коде. Эвристика чинила бы собственный баг наугад, и он уехал бы дальше в T16 |
| строгость фильтра медиа | **узкие структурные маркеры + вычитка 30/30** | правило по одним плохим примерам систематически режет здоровое — что вычитка и показала на пяти вопросах из тридцати |
| глубина `acceptance` | **нормализация + скобки + разделители → `acceptVariants[]`** | результат лежит в данных явным массивом: виден глазами и покрыт тестами. Морфология (pymorphy2) отклонена — объём работы ради подсказки, которая по T2a и так никогда не блокирует выбор игрока |

##### Что осталось открытым

- **`data/*_clean.json` лежат в гите.** `gq_clean.json` — 12 МБ производных данных, регенерируется из сырого дампа за 0.6 с. Решено коммитить: сырые дампы в репозитории уже лежат, и приложению чистый файл понадобится ассетом. Если репозиторий решат не раздувать — файл уходит в `.gitignore`, а CI прогоняет санитайзер перед сборкой.
- **Ассеты Flutter.** Чистые дампы лежат в `data/` в корне репозитория, а Flutter не пускает ассеты за пределы каталога пакета. T2a придётся решить: копировать их в `app/assets/` шагом сборки или сразу генерировать туда санитайзером.
- **Что осознанно НЕ чинится в ответах, и почему** (проверено 30.08.2026 на вычитке T16):
  - *непарная кавычка в `acceptVariants`* — `группа "pink floyd`, 522 случая. Не дефект: матчер в `app/lib/cycle/answer_match.dart` заменяет всю пунктуацию на пробел **с обеих сторон** — и в варианте, и во вводе игрока, — так что строки совпадают. Кривизна видна только в JSON. Чинить нечего;
  - *инструкция чтецу, уехавшая в ответ* — `[Перед оглашением можно объявить начало 3-го тура. :-)] На обрыве скалы` — **2 случая на ~11 000**. Правило «срезать ведущую квадратную скобку» сломало бы `[Руаль Энгельбрегт Гравнинг] Амундсен` и `[Бланк, надорванный с любой стороны]`, где скобка несёт смысл. Цена починки выше цены дефекта.

- **Вычитка 30/30 — не автотест.** Она сделана и её находки закреплены регрессиями в `test_sanitize.py`, но при следующем изменении правил её надо повторять руками: `python3 scripts/sanitize_dumps.py <сид>` кладёт свежую выборку в `data/sanitize_sample.json`.

#### ✅ T16 — импорт индекса бинго (поглотил T13)

**Закрыта 30.08.2026.** Все семь шагов сделаны, вычитка проведена, картинки скачаны полностью. Гейт закрытия проверен командой: `git merge-base --is-ancestor 3f83edb origin/main` — да.

| | было | стало по факту |
|---|---|---|
| статей взято | — | **199 из 199**, ноль ошибок |
| вопросов в бинго-корпусе | 599 | **1764** годных из 1938 (вики 599 + индекс 1165) |
| тем | 132 | **328** |
| тем с 3+ вопросами | 103 | **286** |
| раундов по 5 | 119 | **352** |
| картинок | — | **771 из 771**, 62.9 МБ, ни одной недоступной |
| раздаток | 0 | **105** локально, 110 вопросов индекса помечены `handout` |
| тем в обоих источниках | «одна и ~8 нечётких» | **ровно одна**, «Сумбур вместо музыки» |

Числа печатает `python3 scripts/bingo_index_report.py` → `data/bingo_index_report.json`.

##### Вычитка 30.08.2026 — что нашла

Проведена на новом сиде (`python3 scripts/sanitize_dumps.py 20260830`) плюс отдельная выборка 20/20 только по индексу — общая выборка на 80% состоит из gq и про T16 говорит мало.

- **Найден настоящий баг:** в notion строка турнира идёт ПОСЛЕ метки «Вопрос 35», и её текст уезжал в начало вопроса. Починено, закреплено тестом.
- **Отбраковка не режет здоровое:** из 20 отбракованных записей индекса все 20 действительно требуют картинку либо являются дуплетом.
- ⚠️ **Известное пере-исключение: 48 вопросов.** Правило «картинка в теле вопроса → `handout`» ловит и те случаи, где автор статьи просто поставил иллюстрацию рядом, а вопрос берётся и без неё («Заполните ПРОПУСК двумя словами»). Из 56 вопросов, исключённых только по картинке, текст явно ссылается на неё лишь у 8. Оставлено намеренно: бинго-корпус в приложение пока не едет вовсе, так что цена сегодня нулевая, а T3 всё равно вернёт их все, научившись показывать раздатку. Обратная ошибка — показать игроку вопрос без нужной картинки — стоит дороже.
- **Два мелких дефекта санитайзера, не от T16** — назвать, но не чинить здесь: инструкция чтецу в квадратных скобках срезается только из текста вопроса, но не из ответа (`ix-kliffhenger-4`: `[Перед оглашением можно объявить начало 3-го тура. :-)] На обрыве скалы`), и `normalize_answer` оставляет непарную кавычку внутри ответа (`ix-meiflauer-3`). В обоих случаях правильный вариант в `acceptVariants` тоже есть, так что матч не ломается.

✅ **Картинки телеграфа докачаны 30.08.2026 под VPN.** Инкрементальность подтвердилась на живом прогоне: 565 уже скачанных пропущены по совпадению `sha256`, тронуты только 206 недоступных. Одна картинка wikimedia взялась со второй попытки — 429 действительно был транзиентным, не блокировкой. Итог: **771 из 771, ни одной недоступной**, 62.9 МБ, все 105 раздаток на месте.

**Постановка проработана 30.08.2026.** Все четыре хоста проверены живым запросом, объём и вес замерены выборкой. Пять оценок прежней карточки оказались неверны — исправлены ниже и помечены ⚠️.

**Источник:** «Индекс разрешенных бинго» — 199 клише со ссылками.
`https://nine-workshop-034.notion.site/2c0170d6c19c80fcb454c5607b05b927`

✅ **Шаг 1 сделан 29.08.2026** — индекс извлечён в `data/bingo_index.json` (199 записей, поля `name / emoji / url / host`). Заново ходить в Notion не нужно, дальше работа идёт от этого файла.

##### Как сейчас

- Цепочка бинго: `data/bingo_wiki_dump.json` (139 тем) → `scripts/structure_bingo_dump.py` → `data/bingo_questions.json` (647) → `scripts/sanitize_dumps.py` → `data/bingo_clean.json` (**599** годных, **132** темы).
- Краулера по статьям индекса нет: в `scripts/` ноль упоминаний `coolbingo`, `telegra`, `notion`, `teletype`.
- **Бинго-корпус в приложение не едет вообще** — `scripts/build_app_assets.py` читает только `gq_clean.json`. Поэтому T16 действительно скриптовая: `app/` не трогается ни одной строкой.
- `data/` коммитится целиком (репо 4.6 МБ). Ассеты приложения — нет (`.gitignore`: `app/assets/*.json`).

##### Разведка 30.08.2026: что отдаёт каждый хост

| хост | статей | текст | картинки |
|---|---|---|---|
| vk.com | 133 | ✅ обычный `curl`, **cp1251**, контейнер `div.article_view` | ✅ `sun9-*.userapi.com` |
| telegra.ph | 63 | ⚠️ HTML режется на TLS-хендшейке (`Connection reset by peer`), но **`api.telegra.ph/getPage?path=<slug>&return_content=true` работает** | ❌ `telegra.ph/file/*` недоступен, в web.archive его нет (403) |
| notion.site | 2 | ✅ `api/v3/loadPageChunk`, pageId с дефисами | ⚠️ `attachment:<uuid>:image.png` — нужен резолв подписанного URL |
| teletype.in | 1 | ⚠️ хост недоступен, но **снапшот есть в web.archive** (01.10.2025, 20 КБ) | из снапшота |

**Поправки к прежней оценке:**

- ⚠️ **Телеграф надо брать API, а не HTML-парсером.** `api.telegra.ph` отдаёт готовое дерево узлов (`figure` / `blockquote` / `p` / `aside`), парсить нечего — и это заодно обходит блокировку. Отдельного HTML-парсера под телеграф не будет.
- ⚠️ **VK и телеграф структурно одинаковы**: `<blockquote><strong>Вопрос N:</strong> …`, дальше `Ответ: / Зачёт: / Комментарий: / Источник(и): / Автор:`, разделитель `ИЛИ` между вопросами одной темы. Один блочный разборщик закрывает **196 из 199** статей. Парсеров не четыре, а два: блочный (vk + telegraph + wayback-teletype) и notion.
- ⚠️ **Notion — единственное исключение по форме.** Порядок другой (`Вопрос 26` → строка турнира → текст вопроса), а тип блока плавает внутри одной страницы: то `quote`, то `text`.
- ⚠️ **«~8 нечётких совпадений тем» — их нет.** Реальное пересечение вики и индекса — ровно одна тема, «Сумбур вместо музыки». Остальное при проверке оказалось шумом сравнения строк («Алая буква» ↔ «Ё (буква)», 0.71). Слияние тем — сравнение нормализованных имён, развилки нет.
- ⚠️ **Объём меньше заявленного.** Выборка 5 vk-статей: 3 / 14 / 4 / 3 / 6 вопросов; выборка 6 телеграф-статей: ~4. Среднее ≈ 6 → **~1200 вопросов**, не 1400. Картинок 1–11 на статью, ~5 в среднем → **~1000 файлов, 94 МБ** в максимальном разрешении (замерено скачиванием 14 штук, средняя 96 КБ).
- Пересечение нынешнего бинго-корпуса с gq — **9 вопросов из 599** (1.5%). Дедуп нужен, но дешёвый.
- `robots.txt` ни у одного из хостов не объявляет `Crawl-delay`; `/@coolbingo-*` в `Disallow` у VK не входит. Своя вежливая пауза 0.5 с всё равно ставится.

**Что это даёт:**

| | было | станет |
|---|---|---|
| клише | 132 | ~330 |
| вопросов | 599 | ~1800 |
| вопросов на клише | 4.5 | ~5.5 |
| раундов по 5 | 119 | ~360 |

Снимается главное ограничение оси клише — «~4.5 проверок узнавания на тему за всю жизнь, потом тема исчерпана».

**Свежесть:** вики оборвана на 2019 (снапшоты 2016-2019). В статьях индекса вопросы идут по ЧР-2026 включительно. Клише последних семи лет — здесь.

**T13 (раздаточный материал) закрыт слиянием сюда.** Он был списан, потому что все ссылки вели на мёртвые `chgk.zaba.ru` и `db.chgk.info`. В статьях индекса картинки живые, по 1-11 на статью, включая раздатки. Раздатка возвращается как побочный продукт импорта, а не как отдельная задача.

##### Шаги

Порядок — по цене переделки: форма записи и провенанс первыми, механическая скачка последней.

1. **Нормализованный блок — общая форма для всех источников.** `scripts/bingo_index_blocks.py`: три добытчика (`fetch_vk`, `fetch_telegraph`, `fetch_notion`) приводят статью к одному списку блоков `{"type": "p"|"quote"|"aside"|"heading"|"figure"|"list", "text": str, "images": [{"src": str, "caption": str|None}]}`. Дальше по конвейеру идёт только эта форма — разборщик вопросов не знает, из какого хоста пришёл блок.
   Запись статьи в дампе: `{name, emoji, url, host, fetcher, fetcherVersion, blocks, error?}`. **`fetcherVersion` — по образцу `DECODER_VERSION` в `scrape_bingo_wiki.py:18`:** «скачано» и «скачано годным кодом» — разные вещи, и без отдельного поля их не различить.
   → проверка: тест на трёх сохранённых фикстурах (по одной на добытчика) в `scripts/tests/fixtures/` — все три дают непустой список блоков одной формы; тест без сети.

   **Сделано 30.08.2026** — `scripts/bingo_index_blocks.py`, `scripts/tests/test_bingo_index.py` (34 проверки). Живой прогон `fetch_article` прошёл по одной статье на каждый хост. Три находки:
   - ⚠️ **VK отдаёт статью только браузерному User-Agent.** С проектным UA приходит **статус 200**, 123 КБ и страница «Памылка» — в другой кодировке и без контейнера статьи. То есть неверный UA выглядел бы не как ошибка, а как пустая статья. Отсюда `VK_HEADERS` с браузерным UA и `parse_vk`, возвращающий `None`, а не `[]`. Записано в `pitfalls_scraping.md`;
   - **картинки Notion резолвятся и качаются** — `attachment:<uuid>:…` через прокси `/image/…?table=block&id=&spaceId=` отдаёт 200 и 265 КБ PNG. Оговорка «если резолв не выйдет» из шага 5 снята;
   - **раздатки в статьях действительно есть** — в notion-статье «Альберто Джакометти» лежит внешняя ссылка `gotquestions.online/pics/…_razdatka.png`, и она проезжает нетронутой.

2. **Краулер `scripts/crawl_bingo_index.py`** → `data/bingo_index_dump.json`. Инкрементальный, ровно как `scrape_bingo_wiki.main()`: по умолчанию идёт только за несвежими (`fetcherVersion` не текущий или `blocks` пуст), неудачный запрос **не имеет права затереть** уже взятые блоки, отступ с ростом `(5, 15, 45)` через `polite_get`, пауза 0.5 с. Сохранение через аналог `save_if_not_worse` — прогон, ухудшивший число взятых статей или объём прозы, ложится рядом в `.new.json`, а не поверх.
   Маршрутизация по `host` из `bingo_index.json`; `teletype.in` идёт через уже написанный `scrape_bingo_wiki.best_snapshot_url` (сортировка снапшотов по `length`, не по дате).
   → проверка: два прогона подряд — второй печатает «уже свежих 199, идём за 0» и не делает ни одного запроса; принудительно испорченный `fetcherVersion` у одной статьи возвращает ровно один запрос.

3. **Разборщик `scripts/structure_bingo_index.py`** → `data/bingo_index_questions.json`, поля один в один как у `structure_bingo_dump.py` (`id / theme / tournament / question / answer / acceptance / comment / sources / author`) плюс `source: "index"`, `articleUrl`, `handoutImage`. Иллюстрации темы на строку вопроса **не кладутся** — они принадлежат статье, а не вопросу, и живут в манифесте `bingo_images.json` под ключом `articleName`; оттуда их и возьмёт справочник T14.
   - **`id` = `ix-<slug>-<n>`.** Префикс обязателен: тема «Сумбур вместо музыки» есть в обоих корпусах, и без него id столкнутся.
   - Якоря по префиксам меток, как в `structure_bingo_dump.py:10-13` (`Ответ`, `Зачет`/`Зачёт`, `Комментарий`, `Источник`/`Источник(и)`/`Источники`, `Автор`/`Автор(ы)`).
   - ⚠️ **Строка турнира сама содержит двоеточие** — `Кубок Гармонии. Элемент Шестой: Магия · Июнь 2022`. Наивный разбор «всё до первого `:` — метка» разрежет её пополам. Турнир опознаётся позицией (блок перед `Вопрос N`), а не двоеточием.
   - `Источник(и):1. …` встречается **без пробела** после двоеточия — регулярка не должна его требовать.
   - Разделитель `ИЛИ` в отдельном блоке (`aside` в телеграфе, `text` в notion) закрывает предыдущий вопрос.
   - **Статьи с нулём извлечённых вопросов печатаются поимённо**, как `structure_bingo_dump.py` печатает `zero`. Молчаливый ноль — это ровно тот сценарий, на котором T1 потерял три темы из 139.
   → проверка: красный→зелёный на фикстурах — разбор статьи со строкой турнира, содержащей двоеточие, даёт один вопрос с целым `tournament`; на наивной реализации тест падает. Плюс: суммарно извлечено ≥ 1000 вопросов, статей с нулём — список в отчёте.

4. **Привязка картинок — структурная, не по догадке.** Картинка, стоящая **внутри тела вопроса** (между блоком турнира и блоком `Ответ:`), — это раздатка: едет в `handoutImage` того вопроса. Все прочие картинки статьи — иллюстрации темы: в строку вопроса не попадают, остаются в манифесте под своей статьёй (материал для справочника T14).
   ⚠️ **URL картинки не имеет права попасть в текст вопроса.** `sanitize_dumps.RE_MEDIA` бракует по подстрокам `.jpg` / `.png` / `/pics/` в сыром тексте — вклеенная в `question` ссылка отправит вопрос в `excluded: media` молча и целиком. Картинки живут только в своих полях.
   → проверка: тест — вопрос с картинкой в теле получает непустой `handoutImage` и `excluded == "handout"` (не `media`); вопрос с картинкой после блока `Автор:` не получает `handoutImage` вовсе.

5. **Скачка картинок `scripts/fetch_bingo_images.py`** → файлы в `data/images/`, манифест `data/bingo_images.json`: `{url, host, file, sha256, bytes, width, height, status: "ok"|"unreachable"|"error", articleName, attachedTo, isHandout}`.
   - **Раздатка (`isHandout: true`) сохраняется как есть** — исходное разрешение и формат. Ужимаются только иллюстрации: ширина ≤1024, JPEG q85 через Pillow (**12.2.0, уже установлен — проверено**), ожидаемо 30-40 МБ вместо 94. Раздатка часто скан с мелким текстом или схема, которую игрок обязан прочитать; ужатая до нечитаемости, она выяснится только на живой игре в T3, когда оригинала уже не будет.
   - **VK может ответить капчей или логин-стеной** где-то на 133 запросах — сейчас отдаёт без авторизации, но гарантий нет. Отдельного обхода не пишем: краулер инкрементален, прогон продолжится с той же точки, когда стена спадёт. Заворот в HTML-логин обязан считаться ошибкой, а не пустой статьёй, — иначе статья запишется «взятой» с нулём блоков.
   - **VK отдаёт превью, если не попросить полный размер:** в `src` сидит `cs=72x0`, и это 4 КБ вместо 166. Максимальную ширину брать из списка `as=` того же URL и подставлять в `cs=<w>x0` (проверено: `cs=1186x0` даёт полный файл).
   - Идемпотентность по `sha256`: файл на месте и хеш совпал — запрос не делается. Повторный запуск добирает только `status != "ok"`.
   - `telegra.ph/file/*` из текущей сети недоступен — эти ~190 записей получают `status: "unreachable"`. Запуск под VPN добирает ровно их.
   - Notion: `attachment:<uuid>:<file>` резолвится через `/image/<url-encoded>?table=block&id=<blockId>&spaceId=<spaceId>`; параметры лежат в том же ответе `loadPageChunk`. Две статьи — если резолв не выйдет, они помечаются `unreachable` и это не блокирует задачу.
   → проверка: `data/images/` непуст, у каждой записи манифеста со `status: "ok"` файл существует и его `sha256` совпадает; повторный прогон печатает «скачано 0, пропущено N»; ни одна иллюстрация не шире 1024 px, **ни одна раздатка не пережата** (байты совпадают с ответом сервера).

6. **Слияние в один корпус и дедуп.** `sanitize_dumps.py`: вход бинго становится списком из двух файлов (`bingo_questions.json` + `bingo_index_questions.json`), строки конкатенируются, каждой проставляется `source: "wiki"|"index"`, выход — прежний единственный `data/bingo_clean.json`.
   **Отклонение от буквы развилки, намеренное:** структуризатор индекса пишет свой файл, а не дописывает в `bingo_questions.json`. Два скрипта, пишущих в один файл, — это ровно тот класс, где один прогон молча стирает результат другого; склейка делается в одной точке, у санитайзера. Для всех потребителей ниже (T3, T14) корпус по-прежнему один.
   Дедуп: нормализованные первые 120 символов вопроса против `gq_clean.json` и против вики-строк. Совпадение помечается `duplicateOf: <id>`, **ничего не удаляется** — принцип T11. Поле не исключает вопрос из корпуса, а даёт T3 возможность не показать в «Бинго» то, что игрок уже проходил в «Классике».
   → проверка: `sanitize_report.json` печатает разбивку по `source`; тем в объединённом корпусе ~330, из них с 3+ вопросами — число в отчёт; `id` уникальны (тест на коллизию).

7. **Отчёт и ручная вычитка.** `data/bingo_index_report.json`: статей взято/не взято по хостам, вопросов на статью, статьи с нулём вопросов, темы, встретившиеся в обоих корпусах, картинок ok/unreachable. Плюс свежая выборка 30/30 в `sanitize_sample.json` — **другим сидом**, чем тот, по которому правились правила.
   → проверка: вычитка 30 отбракованных и 30 пропущенных глазами; находки закрепляются регрессиями в `test_sanitize.py`.

##### Развилки — закрыты 30.08.2026

| развилка | выбор | почему |
|---|---|---|
| один корпус или два | **один `bingo`, поле `source: wiki\|index`, один `bingo_clean.json`** | отдельный третий вход пришлось бы не забыть подключить в санитайзере, отчёте, T3 и T14 — классическое место, где половина корпуса теряется молча. Цена: id обязаны быть с префиксом, иначе столкнутся на общей теме |
| хранение картинок | **качать локально, коммитить в `data/images/`; иллюстрации ужимать до 1024 px, раздатку — нет** | «хотлинки на медиа умирают» — этот сценарий уже один раз убил T13. Ужатие иллюстраций даёт 30-40 МБ вместо 94; репо вырастет с 4.6 МБ, и это осознанная цена за то, что копия переживёт удаление на стороне VK. Раздатка выведена из-под ужатия отдельно: её смысл — быть прочитанной |
| картинки телеграфа (63 статьи, ~190 файлов) | **текст сейчас через `api.telegra.ph`, картинки — `status: unreachable`, докачать позже под VPN** | краулер инкрементален по устройству, «докачать потом» — не новый код, а тот же прогон. Прогон целиком под VPN отвергнут: VK отдаёт статьи без авторизации именно с этого IP, а из-за границы часто отвечает 451/geo — можно сломать те 133 статьи, что сейчас работают |
| раздатка и правило санитайзера | **T16 только проставляет `handoutImage`, `excluded: "handout"` не снимает** | «Классика» уже читает корпус и картинку показывать не умеет — снятый брак дал бы игроку «Перед вами…» без раздатки. Снятие исключения — одна строка в T3/T14, когда экран научится показывать картинку до старта минуты |
| парсеры под teletype и notion | **teletype — через готовый `best_snapshot_url` (снапшот в архиве есть), notion — свой разборщик на ~30 строк** | спец-парсер под teletype не нужен вовсе; notion неизбежен, но это 2 статьи из 199, и провал по ним задачу не блокирует |

##### Зависимости

Требует только `data/bingo_index.json` (готов). Никого не блокирует, идёт параллельно фазе 1. **Блокирует T3** (сетка «Бинго» — выросший корпус) и **T14** (справочник — контент и картинки). Приложение не трогает: `build_app_assets.py` бинго-корпус не читает.

Новые зависимости Python: `Pillow` (12.2.0 уже стоит), `lxml` и `requests` уже используются.

⚠️ **Побочный эффект на CI:** в `.github/workflows/build.yml` в `paths:` стоит `data/**`, то есть коммит картинок запустит полную сборку веб-превью и APK, хотя приложение к ним не обращается. Приемлемо (сборка идёт раз), но чекаут репозитория после T16 станет заметно тяжелее для всех последующих прогонов.

##### Критерий успеха

- **красный→зелёный, без сети, на фикстурах:** (а) статья со строкой турнира, содержащей двоеточие, даёт вопрос с целым `tournament` — наивный разбор «до первого двоеточия» тест валит; (б) вопрос с картинкой в теле получает `handoutImage` и `excluded == "handout"`, а не `media` — реализация, вклеивающая URL в текст вопроса, тест валит; (в) `id` вики и индекса не сталкиваются на теме «Сумбур вместо музыки» — реализация без префикса `ix-` тест валит;
- **идемпотентность живым прогоном:** второй запуск краулера делает 0 запросов, второй запуск скачки картинок скачивает 0 файлов;
- **числа, которые надо вписать сюда фактом после прогона:** статей взято из 199 (по хостам), вопросов извлечено, тем в объединённом корпусе, тем с 3+ вопросами, картинок ok / unreachable, вес `data/images/` в МБ;
- **не автотест, делается руками:** вычитка выборки 30/30 из `sanitize_sample.json` на новом сиде. Правило, выведенное из одних плохих примеров, режет здоровое, и увидеть это можно только глазами;
- задача не считается закрытой, пока число статей с нулём извлечённых вопросов не разобрано поимённо: либо статья действительно без вопросов, либо это дыра в разборщике.

**UI-поддержка раздатки (для T3/T14, не для T16):** картинка показывается **до** старта минуты — раздатка выдаётся до вопроса, а не вместе с ним.

#### 🟢 T15 — ось фактов (эксель)

**Источник:** гугл-таблица, 17 листов, ~1240 записей.
`https://docs.google.com/spreadsheets/d/1iyyf_cTV0aUiQLbajGS4EEp0owuRQYEbeBn2ovpdSMw`

Забирается без авторизации: `export?format=xlsx` (полностью, 8 МБ) либо `export?format=csv&gid=…` (один лист).

**Два типа листов:**

- **готовые пары** `термин → значение → комментарий` (~430): Перифразы 189, Псевдонимы 76, Олимпиады 59, Спорт 29, Прототипы 24, ИЗО 22, Нобелевские 20, Латинские выражения 5, Имена 4. Импортируются почти как есть — нужен маппинг по заголовку, не по индексу (ширина листов 26 колонок при 3-4 заполненных);
- **проза** — абзац-справка на строку (~810): Рандом 540, Литература 81, История 62, Науки 60, Христианство 28, Греки 16, Шекспир 15, Цитаты 9. **Головной термин вытаскивается регуляркой в 98% случаев** (`Термин — описание`), то есть проза тоже конвертируется в пары.

**Механика — карточки.** `Архангельский мужик → ?` → `Ломоносов`. 189 перифразов уже колода, проектировать нечего; тот же экран работает на псевдонимах, прототипах, латыни и олимпиадах без правок.

**Здесь же место Leitner.** Из T4b он снят (требовал, чтобы все 19 приёмов уже существовали), а 430 пар — канонический случай интервального повторения: бинарный вердикт, разметки не нужно, алгоритм подходит без натяжек.

**Издержки, честно:**

1. **вычитка обязательна** — в записи про «Лисистрату» сидит вандализм из источника («отказываются ебаться сексом»). Прозаические листы — копипаста лидов Википедии, такое там будет ещё. В приложение уедет как есть, если не прочитать глазами;
2. лист «Рандом» — 540 записей, 44% всего, без домена. Либо разложить, либо честно оставить категорией «разное»;
3. **9 встроенных картинок, 6 МБ** — живые репродукции («Остров мёртвых» Бёклина, «Телефон-Омар» Дали), привязка к строке через якоря `xl/drawings/*`. Извлекаются.

#### 🟢 T6 — «Своя игра»

Категории × номиналы очков. **Не в MVP**, отдельная большая задача.

Контент есть: гугл-док «Архив Свояка Крафт», 66 КСИ + 77 ИСИ тем с готовыми пакетами по ссылкам. Механически похож на «Бинго» (обе — сетка, тап по клетке → вопрос), но достаточно отличается, чтобы не тащить заодно.

---

## Права на контент

Вопросы выложены в открытый доступ с условием некоммерческого использования — приложение бесплатное и некоммерческое, вопрос закрыт.

Атрибуция всё равно нужна и данные для неё есть (автор у 95% вопросов, источники у 82%) — экран раскрытия показывает источник и автора по умолчанию.

**Открыто:** эксель и статьи индекса — работа одной команды (авторы вопросов в статьях — Сергей Лобачёв и другие, он же поздоровался первой строкой на листе «Рандом»). Раз контент поедет в раздаваемое приложение, стоит перекинуться с ними словом. Вопрос один, а не два.

---

## Анти-бэклог — чего не делать

Записано наравне с задачами, чтобы не возвращаться к обсуждению:

- командный и онлайн-режим, аккаунты, облачная синхронизация, лидерборды;
- XP, уровни, очки — см. T9;
- **генерация вопросов LLM** — соблазн максимальный, качество ЧГК-вопроса LLM не вытягивает, а один плохой сгенерированный вопрос обесценивает весь корпус в глазах игрока. LLM допустима **только как ассистент разметки** с ручной проверкой;
- новые источники контента до конца фазы 1. Контента уже больше, чем приложение способно переварить.

---

## Состояние контента — замерено 29.08.2026

Все цифры проверены по самим дампам, а не по памяти.

### `data/gotquestions_dump.json` — 9280 вопросов, 150 пакетов

| поле | заполнено |
|---|---|
| `question`, `answer` | 100% |
| `author` | 95% |
| `sources` | 82% |
| `comment` | 70% |
| `complexity` | 32% |
| `acceptance` | **28%** |

Уникальных вопросов 9273 (7 почти-дублей). Пересечение с бинго-дампом — 10 вопросов.

⚠️ **`complexity` — не «два числа», как записано в первой редакции T5.** Из 2943 непустых у **2082 одно число, у 861 два**. Пары коррелируют 0.78, средняя разница 12 п.п., обе в диапазоне 0-100 со средним ~50 — похоже на «% взятия по двум группам команд», но **именно похоже**. Показывать «этот вопрос взяли только N% команд» на основании *похоже* нельзя: это фактическое утверждение о турнире. Либо сверить с известным пакетом до релиза, либо формулировать относительно — «сложнее, чем 70% вопросов базы».

### `data/bingo_questions.json` — 635 вопросов / 135 тем

`comment` 92%, `sources` 97%, `author` 98%, `acceptance` **31%**.
После санитайзера и без метатем — **595 / 130**.

⚠️ **52% ответов (330 из 635) буквально содержат название темы.** «15 минут славы» → «Пятнадцать минут славы»; «1984» → «1984». Отсюда порядок шагов в T3: сетка показывается только после того, как версия сдана, иначе половина корпуса раздаёт ответ списком из девяти.

**Частотность как бесплатная лесенка сложности.** Число вопросов на тему = как часто клише встречается в турнирах. Кампанию начинать с частотных — они и попадутся раньше, и перепроверять есть чем:

```
15 Москва-Петушки   14 Белое солнце пустыни   14 Павлов   13 Мёртвые души
11 Гаммельнский крысолов   11 Крик   10 451 градус по Фаренгейту
…
 1 Это'О, Самуэль · Точка над i · Одностишие · Мадлен · Бендер · и ещё 5
```

Ни `complexity`, ни ручной работы не требует.

### `data/bingo_wiki_dump.json` — 139 тем, проза статей

**65 КБ прозы, статья есть у 135 тем из 139**, медиана 397 символов. `structure_bingo_dump.py` её выбросил — забирается в T14.

Формат статьи — ровно нужный, вот «Ковентри» по структуре:

```
[факт]            Небольшой английский город Ковентри появляется
                  в вопросах по трём разным причинам.
[подзаголовок]    Немецкая бомбардировка во время второй мировой
[как обыгрывают]  Вопросы обыгрывают либо сам факт уничтожения,
                  либо последующее восстановление с нуля.
```

Снапшоты web.archive: 2018 — 80 тем, 2019 — 31, 2017 — 19, 2016 — 7. **Корпус заморожен на 2019**, поэтому индекс T16 и нужен.

### Авторазметка приёмов не работает — проверено

Расчёт «комментарии часто прямо называют приём» не подтвердился. Греп по `comment`:

| приём | совпадений |
|---|---|
| «ложный ход» буквально | **0** |
| обманка / уводит в сторону | 7 |
| подсчёт сущностей | 5 |
| учесть имя | 3 |
| тавтология | 2 |
| аналогия | 77 |
| замена | 1053 |

**Ядро таксономии — 12 подтипов ложного хода — не находится вообще:** автор вопроса не пишет «здесь ложный ход», он пишет разбор по существу. А 77% всего пула маркеров это «замена», из которых **66% — техническое «мы заменили X на Y»**, конвенция оформления, а не приём рассуждения.

Вывод: разметка приёмов **ручная от начала до конца**. Отсюда «приём недели» с разметкой из игры (T4a) вместо разметки как отдельной работы.

Пересечение «маркер + `complexity`» — 606 вопросов, если понадобится сортировать по сложности внутри приёма.

---

## История решений — что переигрывалось и почему

Записано, чтобы не возвращаться по кругу.

**Механика «Бинго» менялась дважды.**
Первая редакция: сетка тем → тап по клетке → вопрос этой темы. Отменена: тема известна до вопроса, то есть узнавание клише — ради которого режим и существует — выключено полностью.
Вторая: вопрос → выбрать одну из 9 тем. Отменена: 52% ответов содержат название темы, выбор из девяти раздаёт ответ.
Текущая: вопрос → своя версия → и только потом выбор клетки. Ближе к изначальному замыслу, чем обе предыдущие: узнавание работает и ответ не утекает.

**Самооценка: бинарная → тройная.** «Верно / не ответил» после показа ответа — хиндсайт-байес. Этим числом кормились порог 70% в T3, «% верных» в T9 и подбор в T4. Средняя корзина «почти» диагностически самая ценная.

**XP и уровни сняты.** Крючок NYT Games — не XP, а один конечный объект на сегодня. Плюс начисление «за верный ответ» опиралось на бинарность, которой больше нет.

**Приём стал тегом вопроса, а не режимом.** Разметка дорогая → должна работать везде: подсказка в Классике, причина промаха, карта слабых мест. «Тренажёр рассуждений» превратился в плейлист поверх общего движка, а затем — в «приём недели» на общих рельсах.

**T4 сначала задвинули, потом вернули.** Задвинули за дорогую разметку — аргумент оказался плохой: у клише разметка ровно такая же ручная. Настоящее различие в обратную сторону: клише конечны (~330 и всё), приёмы применимы почти к любому вопросу из безграничного корпуса. Учебник — это то, на чём концепт живёт после того, как кампания кончилась. Без него у приложения истекает срок годности ровно в этот момент.

**T13 воскрешён и слит в T16.** Был списан за мёртвые ссылки на картинки. В статьях индекса картинки живые — раздатка возвращается побочным продуктом импорта.

**Три источника, три непересекающиеся роли:** вики — костяк кампании вширь, замороженный на 2019; индекс — глубина, свежесть и картинки, растёт; эксель — ось эрудиции.

---

## Конвенция при параллельной работе

Если берёшь задачу в отдельном окне или сессии — отметь в статусе 🔧 и своё имя/окно, чтобы не столкнуться с другой сессией. Закрытую задачу помечай ✅ с датой.
